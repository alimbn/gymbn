# gyMbn — Gym Takip PWA

## Hedef
Hocanın haftalık verdiği antrenman programını ve kullanıcının gerçekte yaptığını takip eden, telefona kurulabilen offline PWA. Şu an manuel tutulan Excel'in yerini alacak; hocaya "ne verildi / ne yapıldı" kıyasını göstermeyi kolaylaştırmak asıl amaç.

## Gelecek Vizyonu (henüz plan yok, sadece kayıt altında — 2026-08-08)
Kullanıcının kendi sözleriyle belirttiği, uzun vadeli yön (şu an hiçbir tasarım/plan çalışması yapılmadı, sadece unutulmasın diye kayıt altına alınıyor):
1. **Hocaya da bu uygulama üzerinden bir ekran/erişim verilecek** — şu anki "Haftalık Özet" ekranı (bkz. Kararlar) bunun ilk, en basit adımı (kullanıcı ekranı gösterip hocaya anlatıyor); ama uzun vadede hocanın kendi girişi/görünümü olması hedefleniyor.
2. **Daha da ileride:** uygulama, başka hocaların ve öğrencilerin de kendi hesaplarıyla girebileceği çok-kullanıcılı bir yapıya doğru evrilebilir (bir tür platform). Bunun ne zaman/nasıl yapılacağına dair henüz bir karar yok — mevcut mimari (tek paylaşılan Firebase hesabı, `users/{uid}/data/main` tek doküman) bunun için yeterli değil, ciddi bir yeniden tasarım (gerçek çoklu-kullanıcı veri modeli, roller — hoca/öğrenci —, izin/erişim kuralları) gerektirecek. Şimdilik sadece yön olarak not düşülüyor; günü gelince ayrı bir Plan Mode turu gerekecek.
3. **Rol bazlı ekranlar (2026-08-08 eklenen detay):** Yukarıdaki platform fikri biraz daha somutlaştı, kullanıcının kendi tarifiyle en az 2 farklı rol/ekran öngörülüyor:
   - **Admin ekranı:** kullanıcının (yani mevcut tek hesabın) hocaları ve onlara bağlı üyeleri görüp takip ettiği bir üst-yönetim arayüzü.
   - **Hoca ekranı:** öğrenci tanımlama, ödeme takip, program ekleme (yani bugün kullanıcının kendisinin yaptığı işlerin çoğu, ama HOCA tarafından, birden fazla öğrenci için).
   Bu hâlâ sadece bir yön/niyet kaydı — veri modeli, roller, izinler için henüz hiçbir tasarım kararı alınmadı, madde 2'deki gibi ayrı bir Plan Mode turu gerekecek.
4. **Serbest fikir turu (2026-08-14 eklendi, henüz plan/tasarım yok):** Kullanıcı "dünyadan örnekler" isteyip beyin fırtınası yaptı, dört yeni yön ortaya çıktı:
   - **İlerleme grafiği** — bir egzersizin ağırlığının/hacminin zaman içindeki değişimi (koçun en sık sorduğu "ilerleme var mı" sorusuna cevap, şu an sadece metin bazlı "son sefer" kıyası var). Kullanıcı: "gerçekten önemli, hem hoca hem bireysel takip için, motivasyon da olur."
   - **Boy/kilo/vücut ölçümleri takibi** — hocanın öğrencisi için girebileceği bir alan, ilerleme grafiğinin doğal tamamlayıcısı (performans ekseni + fiziksel değişim ekseni).
   - **Ölçüm girişi için interaktif silüet fikri** (erkek/kadın, bölgeye dokununca o bölgenin ölçüsünü giriyorsun) — kullanıcı önerdi, asistan gerçek maliyetini (özel SVG + hassas dokunma bölgeleri + responsive hizalama) düz bir form listesine kıyasla flag'ledi; kullanım hızı açısından da net üstünlüğü olmayabileceği belirtildi. Kullanıcı henüz karar vermedi, "istersen önce demo yapıp bakarız" ile açık bırakıldı.
   - **Perfect Day / Perfect Week** — bir günün TÜM egzersizleri ✓ ise "Perfect Day", bir haftanın tüm günleri öyleyse "Perfect Week" — rekor (PR) fikriyle aynı aile, veri zaten `inst.status`'ta mevcut olduğu için ucuz bir ekleme, mevcut kutlama/konfeti diline bağlanabilir.
   Ayrıca kullanıcı yeni bir kalıcı kural koydu: **her sohbette asistan kendiliğinden 1-2 yeni animasyon/UI fikri önerecek** ("dünyada trend bu, yeni bu var gibi") — bkz. kalıcı hafıza `feedback_suggest_ui_ideas_each_chat`.
5. **"Yapılan" set satırlarında odak — akordiyon yönü ve renk kararlaştırıldı (2026-08-14, henüz UYGULANMADI, "sonra" statüsünde):** Kullanıcının kendi önerisiyle çıktı — çalışılan sete odaklanmak için ya soluklaştırma ya akordiyon (tamamlanan set küçülüp özete iniyor, egzersiz kartlarındaki mevcut akordiyonun bir seviye içerisi). İki Artifact turunda netleşti:
   - **Yön: Akordiyon.** Kapalı/özet satırlar bilerek çerçevesiz/çizgili kalıyor, AÇIK (aktif) set kendi zeminine (`--surface-2` + çerçeve) oturuyor — "kutu = şu an buradasın" sinyali, input/textarea'ların zaten kullandığı yüzey diliyle tutarlı.
   - **Aktif set çerçeve rengi: amber sol ray (kalın değil, ince bir bant, tam çerçeve değil).** 5 varyant kıyaslandı (nötr gri / tam amber çerçeve / bakır-`--primary` çerçeve / amber sol ray / amber glow) — kullanıcı **amber sol ray**'ı seçti. Asistanın önerisi bakır çerçeveydi (amber zaten "nötr/henüz işaretlenmedi" durumu için kullanılıyor, anlam çakışması riski) ama kullanıcı sıcak/amber hissini tercih etti — bilinçli bir tercih, uygulanacak.
   - **Saklanan diğer varyantlar (başka bir yerde/zaman kullanılabilir):** tam amber çerçeve, bakır (`--primary`) çerçeve, amber yumuşak parıltı (glow) — kullanıcı: "çok güzel, belki bu varyasyonları sonra değerlendiririz ya da başka yerde kullanırız."
   - Demo: https://claude.ai/code/artifact/67c947bd-adfd-4bb8-9b7a-a0a262054596 (canlı, tıklanabilir — soluklaştırma alternatifi de aynı sayfada duruyor, karşılaştırma için).
   - **Durum: UYGULANDI, sonra CANLI KULLANIM sonrası DÜZELTİLDİ (aynı gün).** İlk sürüm: `js/components/setRows.js` yeniden yazıldı, aynı anda sadece bir set açık, diğerleri tek satırlık özete iniyordu (dokununca tekrar açılan). Kullanıcı canlıda gerçekten kullanınca geri bildirdi: **"sadece aktif set'in açık olması, diğerlerinin kapalı gelmesi biraz ux'i azalttı bence. hepsi yine açık gelsin."** — teoride mantıklı gelen bir fikir gerçek kullanımda tersine döndü, tipik bir "demo onayı ≠ gerçek kullanım onayı" örneği. **Düzeltilmiş nihai davranış:** TÜM setler HER ZAMAN tam açık/düzenlenebilir kalıyor (özet/gizleme tamamen kaldırıldı) — SADECE hangi setle uğraşıldığı amber sol ray + kendi zeminiyle işaretleniyor, hiçbir içerik gizlenmiyor. Aktif işaretleme artık iki yönlü: (1) bir setin HERHANGİ bir alanına dokunmak o seti aktif işaretliyor (satır üzerinde tek bir click-delegasyonu, `stopPropagation()` bilerek kaldırıldı ki iç butonlardan da yukarı kabarcıklanabilsin), (2) RIR alanına dokunmak AYRICA bir sonraki bitmemiş sete otomatik ilerletiyor (ağırlık/tekrar tek başına ilerletmiyor). `activate()` artık `renderAll()` DEĞİL, sadece `.active` class'ını taşıyor — yazarken satır aktifleşip tüm liste yeniden render edilseydi input odağı/imleci kaybolurdu. Artık dead code olan `syncLaterRows()` kaldırıldı (carry-forward veri seviyesinde zaten çalışıyor). Yerel test harness'ıyla 19 yeni senaryo doğrulandı (hiçbir satırın asla gizlenmediği, dokunmanın amber'i taşıdığı, rir'in ilerlettiği, CSS'in sadece `.active`'te göründüğü dahil) — hepsi PASS, konsol sıfır hata.

## Yol Haritası (2026-08-09)
Kullanıcıyla mevcut notlardan çıkarılan 6 adaylık bir listeden öncelik sırası netleşti (kullanıcının kendi sıralaması):
1. **Admin ekranı** — kullanıcının hocaları ve onlara bağlı öğrencileri üstten görüp takip ettiği arayüz (bkz. Gelecek Vizyonu madde 3).
2. **Hoca ekranı** — öğrenci tanımlama, ödeme takip, program ekleme; hocanın kendi girişi/görünümü (bkz. Gelecek Vizyonu madde 1 ve 3).
3. **Haftalık Özet / Masaüstü Özeti iyileştirmeleri** — henüz somut fikir yok (bkz. Sonraki Adımlar).

**Sırası henüz belirlenmemiş, listede kalan diğer maddeler** (kullanıcı: "diğerleri listede kalmaya devam edebilir"): özel domain bağlama, "Planlanan (Hoca)" bloğunu kilitleme (ikisi de bkz. Sonraki Adımlar), ve çoklu-kullanıcı mimari temeli (bkz. Gelecek Vizyonu madde 2 — Admin/Hoca ekranlarının teknik ön koşulu, henüz hiç tasarım çalışması yok).

Henüz hiçbir tasarım/uygulama başlamadı — bu sadece öncelik sırası kararı.

**Güncelleme (2026-08-14):** Madde 1 (Admin) ve madde 2'nin Faz A'sı (Hoca: öğrenci tanımlama + program atama) için kod yazıldı ve yerelde doğrulandı — bkz. aşağıdaki "## Admin + Hoca Ekranları, Faz A" bölümü.

**Güncelleme (2026-08-15):** Canlıya alındı — Console'daki gerçek kurallar taslakla karşılaştırıldı (mevcut `users/{userId}` davranışı birebir korunuyor, sadece yeni collection'lar eklendi), fark yok, publish edildi. Kod (`admin.html`, `coach.html`, `join.html`, `js/admin/`, `js/coach/`, `js/roster/`, `js/shared/`, `firebase.json`, `firestore.indexes.json`, `firestore.rules`) commit `354fab0` ile push edildi, `admin.html` canlıda doğru render oluyor (giriş ekranı). Admin hesabı (`[REDACTED-UID]`) ve `admins/{uid}` dokümanı zaten Console'dan oluşturulmuştu. **Kalan tek adım: kullanıcının kendi admin hesabıyla giriş yapıp hoca daveti oluşturması** — bu adım agent'ın sınırı dışında (gerçek şifre girme/hesap girişi hep kullanıcının kendi aksiyonu).

## İyileştirme Listesi (2026-08-11)
Kullanıcı gerçek kullanımdan sonra 11 maddelik bir iyileştirme/hata listesi verdi, sıralanıp netleştirildi. **Kural: tek seferde hepsi yapılmıyor — her madde uygulanıp doğrulanıyor, kullanıcı onaylayınca bir sonrakine geçiliyor** (bkz. kalıcı hafıza `feedback_iterative_batches`).
1. **Kronometre boyutu (1.5x) + çalışırken nabız animasyonu** — dinlenme kronometresi görsel iyileştirme.
2. **Sticky header scroll düzeltmesi** — "nasıl gitti" sonrası otomatik açılan kart, sticky header'ın altında kalıyor.
3. **Tekrar/Rir için özel seçici** — native `<select>`'in iki sorunu birden (aynı değeri tekrar seçememe + dropdown'un erişilemez yerde açılması) kökten çözülecek.
4. **Antrenman başlat/durdur + süre takibi** — "Günü Tamamla" ile duracak, süre gösterilip raporlanacak.
5. **Hoca notlarının ayrı alana taşınması** — `bulkParse.js`'in parantez-içi hoca ifadelerini "Not" yerine hocanın kendi alanına (prescribed) koyması.
6. **Not ikonu** — nota sahip egzersizlerde küçük konuşma balonu göstergesi (madde 5'in doğal devamı).
7. **Egzersiz tipi (ağırlık/süre)** — ayrı bir tasarım turu gerektiren, büyük bir mimari eklenti.
8. **Geri sayım sayacı (plank vb.)** — hedef süreli, son 10sn'de yoğunlaşan nabız animasyonu burada kullanılacak (madde 1'deki sürekli nabızdan farklı olarak, hedefe özel).
9. **"Hayalet hafta" / sıfırlanan geçmiş bug'ı** — kullanıcının isteğiyle en sona bırakıldı ("bu maddeyi log al en son iyileştirmelerden sonra çözelim"), tespiti karmaşık, sıra gelince repro detayları toplanacak.

**Değerlendirilip listeden çıkarılan fikir:** Dinlenme kronometresinin telefonun bildirim alanında (arka planda) canlı güncellenmesi. Statik/backend'siz bir PWA'da bu güvenilir şekilde yapılamaz (gerçek push altyapısı gerekir, projenin felsefesine ters). Kullanıcı bunun yerine madde 1'deki nabız animasyonunu tercih etti.

**Ek madde (2026-08-12, sıraya araya eklenip hemen uygulandı):** Kullanıcı canlıda bir ekran görüntüsüyle fark ettirdi — `statusBadge()`'in solid renk/beyaz ikon stili (rozet olarak) rapor ekranlarında ve kart başlığında doğruydu, ama gün girişindeki asıl "Nasıl gitti?" butonlarında hâlâ eski emoji + translucent-tint stiliydi, tutarsızdı. Ayrıca üçüncü bir "nötr" seçeneği (fena değildi) hiç yoktu, sadece iki durum (✅/🔻) vardı. İkisi birlikte çözüldü: `.status-btn` aktif renkleri `statusBadge()`'le birebir aynı solid renklere (`var(--success)`/`var(--danger)`/yeni `var(--warning)`) + beyaz ikona çevrildi, buton ikonları da rozetle aynı karakterlere (✓/▼/−) geçti (eski ✅/🔻 emoji kaldırıldı), üçüncü bir nötr buton eklendi. `setStatus()` zaten jenerik olduğu için (`next = inst.status === status ? null : status`) nötr otomatik olarak aynı toggle/auto-advance mantığına dahil oldu, ekstra mantık gerekmedi.

## Faz
**Şu an:** Firebase senkron + gerçek auth **canlıda ve kullanıcı tarafından uçtan uca doğrulandı** (haftalık program ekranı hem PC'de hem telefonda aynı veriyi gösteriyor, ekran görüntüsüyle teyit edildi). Yolda bir pürüz çıktı: kullanıcı ilk şifresini unuttu, Firebase'in sıfırlama maili hiç ulaşmadı (kök neden: Admin SDK'nın link üretme özelliği kullanıcı kimlik bilgileriyle çalışmıyor, servis hesabı istiyor) — çözüm olarak hesap silinip yeniden oluşturuldu, Firestore verisi yeni UID'ye elle taşındı (bkz. Kararlar). Uygulamaya ayrıca kalıcı bir "Şifremi unuttum" akışı eklendi ki bu bir daha böyle manuel müdahale gerektirmesin. En son eklenen özellik: **Haftalık Özet** ekranı (bkz. Kararlar) — hocaya haftayı özetleyip göstermek için, "basit özet" olarak başlandı, kullanıcı iyileştirme isteyecek.
**Sonra (karar verildi, henüz başlanmadı):** **Özel bir domain** — sadece daha güzel/kısa bir link için, senkrondan bağımsız, kullanıcı kendi satın alacak (ben ödeme işlemi yapamam). Domain alındıktan sonra GitHub Pages'e bağlama (CNAME dosyası + DNS ayarları) basit bir teknik adım.
**Ayrıca bkz. Gelecek Vizyonu** yukarıda — hoca ekranı ve çoklu-kullanıcı platformu yönünde uzun vadeli bir hedef var, henüz plan yok.

## Kararlar
- **Konum:** Bu proje, kardeş proje **Game-1**'den (ayrı bir YouTube Playables oyunu) tamamen bağımsız, kendi klasöründe.
- **Teknik yığın:** Vanilla HTML/CSS/JS, framework yok, build adımı yok — Game-1'deki sadelik felsefesiyle tutarlı. ES module'ler (`<script type="module">`), bundler yok.
- **Veri saklama:** Tek cihaz, tamamen yerel (localStorage, tek JSON blob `gymbnData`, versiyonlu şema). Backend/giriş yok — kullanıcının açık tercihi: bugün bitirip hemen telefona kurulabilsin.
- **Set girişi UX:** Set başına ayrı satır (stepper +/- ile hızlı ayar + dokunca klavye açan serbest metin alanı). Kullanıcının özel vurgusu: "set başına ayrı satır olabilir ama UI/UX çok iyi olmalı" — bu yüzden auto-prefill (prescribed'dan) + carry-forward (bir seti düzenleyince sonraki dokunulmamış setlere kopyalama) mantığı var, amaç: performans plana uyduysa kullanıcı hiç dokunmadan geçsin.
- **Gün tipleri (Anterior-1 vs.):** Egzersiz kütüphanesiyle aynı mantık — kayıtlı liste, dropdown'dan seçilen, kullanıcı yönetebiliyor (ekle/yeniden adlandır/arşivle).
- **Prescribed (hocanın verdiği) tek satır, actual (gerçek yapılan) set-bazlı dizi.** Hocanın hedefi zaten uniform bir değer; asıl set-set farklılık gerçek performansta oluyor.
- **Ağırlık/tekrar/rir alanları hep string.** Excel'de görülen "15\*15", "23ün 5 altı", "50sn" (süre bazlı) gibi serbest-metin değerleri karşılasın diye — ayrı bir "egzersiz tipi" sistemi kurulmadı.
- **Egzersiz/gün-tipi silme = arşivleme (`archived:true`), gerçek silme değil** — geçmiş kayıtlar id ile referans veriyor, hard-delete onları "hayalet" bırakırdı.
- **Yedekleme (export/import JSON) MVP'ye dahil**, fast-follow değil — local-only depolamanın veri kaybı riskine karşı, maliyeti neredeyse sıfır olduğu için.
- **Ödeme takibi:** Tek tarih yerine ödeme kayıtları listesi (`payments[]`) tutuluyor — neredeyse aynı maliyetle geçmiş de görülüyor. 4 haftalık döngüdeki hafta (1-4) ve gecikme durumu son ödeme tarihinden türetiliyor.
- **Giriş noktası gün değil hafta merkezli (2026-08-05 eklendi):** Dashboard'daki "Bugünkü Antrenmanı Kaydet/Devam Et" → "Bu Haftanın Programını Oluştur/Devam Et" oldu, `#/week` ekranına götürüyor. Haftalık görünüm 7 sabit slot (Pazartesi-Pazar, kullanıcının seçtiği "7 günlük sabit takvim" seçeneği), boş slotlar soluk ama tıklanınca gün oluşturuyor, dolu slotlar özet gösterip `dayEntry`'ye götürüyor. **Şema değişikliği yok** — "hafta" persist edilen bir varlık değil, sadece o haftanın Pazartesi tarihinden (`mondayOfWeek`) hesaplanan navigasyonel bir kavram (Geçmiş'in ay gruplaması gibi). `#/day/new` rotası ve `dayEntry.js`'deki `mode:'today'` dalı kaldırıldı (artık gün oluşturma sorumluluğu `week.js`'in slot tıklama handler'ında). `dayEntry.js`'in geri butonu bu yüzden `history.back()`'e çevrildi (artık hem hafta görünümünden hem Geçmiş'ten açılabiliyor, sabit bir hedefe dönmek yanlış olurdu).
- **Toplu program yapıştırma (2026-08-06 eklendi):** Kullanıcının gerçek kullanım şekli — hocası haftalık programı düz metin olarak atıyor ("Anterior - 1" gibi başlık, altında "egzersiz adı N set X-Y tekrar Zkg" formatında satırlar, aralarında boş satır). Hafta görünümünden "📋 Programı Yapıştır" ile tüm haftayı tek seferde yapıştırıp `js/bulkParse.js`'in (saf, DOM'suz) satır-satır regex zinciriyle ad/set/tekrar/rir/ağırlık/not'a ayrıştırıyor, gün-tipi başlıklarını mevcut kütüphaneyle eşleştiriyor (yoksa yeni oluşturuyor), haftanın boş günlerine otomatik dağıtıyor. **Düzenlenebilir önizleme ekranı** (`js/views/bulkAdd.js`) parse sonrası her şeyi göstermeden commit etmiyor — parse %100 güvenilir olamayacağı için (ör. "(15x15kg)" ağırlık açıklaması mı yoksa not mu, "rir X olana kadar tekrar" gibi serbest ifadeler) bu önizleme güvenlik ağı. Algoritma kullanıcının gerçek 32 satırlık metnine karşı hem Node.js'te hem tarayıcıda birebir test edildi, sıfır hata.
  - **Düzeltme (aynı gün):** Tarih seçimi başta sadece "boş" (hiç kaydı olmayan) günleri listeleyen bir `<select>` idi — kullanıcı zaten bazı günlere (0 egzersizli "kabuk" kayıtlar dahil) dokunmuş olduğu için seçenekler sessizce kayboluyor, kafa karıştırıyordu. Gerçek bir `<input type="date">`'e çevrildi: artık hiçbir tarih gizlenmiyor, hangi tarih seçilirse seçilsin altında "bu günde zaten X var, üzerine eklenecek" bilgisi çıkıyor. Ayrıca: hedef günün zaten bir kaydı varsa ama gün tipi boşsa (kullanıcının "#108" örneği gibi) artık dolduruluyor; zaten bir gün tipi varsa asla sessizce değiştirilmiyor.
- **Gün/hafta silme (2026-08-06 eklendi):** Daha önce hiç silme yolu yoktu (`deleteDayEntry` storage.js'de vardı ama hiçbir ekran çağırmıyordu — kullanıcı "#108" gibi ihtiyaç duymadığı bir günü nasıl sileceğini sordu). `dayEntry.js` başlığına 🗑 butonu eklendi (o günü tamamen siler, `history.back()` ile geri döner). `week.js`'e, o hafta en az bir gün kaydı varsa görünen "Bu Haftadaki Tüm Günleri Sil" butonu eklendi (haftanın 7 gününü topluca siler). İkisi de silinecek gün/egzersiz sayısını gösteren `confirm()` ile onay istiyor.
- **Egzersiz kartları akordiyon (2026-08-06 eklendi):** Kullanıcının isteği: "aynı anda bir ağırlık çalışabilirim, sadece ona odaklanmamı kolaylaştırır." `dayEntry.js`'deki egzersiz kartları artık tek seferde sadece biri açık kalacak şekilde akordiyon (başlığa tıklayınca aç/kapa, yeni bir tanesi açılınca öncekini kapatır). Kapalıyken sadece isim + (oy verilmişse) ✅/🔻 durum ikonu görünüyor — kapalı kartların gövdesi (planlanan alanlar, set satırları, not) hiç DOM'a render edilmiyor, sadece açık olan. Varsayılan: hepsi kapalı başlıyor (kullanıcı isteği — ilk açık gelmesin); yeni egzersiz eklenince o açılıyor. Açık/kapalı durumu kalıcı değil, sadece o ziyaret için.
- **Gün tamamlama işareti (2026-08-06 eklendi):** `dayEntry.js`'de egzersiz listesinin altında "Günü Tamamla" toggle butonu — `entry.completed` (boolean) alanını değiştiriyor. Kullanıcının kendi tabiriyle "bir karşılığı olmak zorunda değil": sadece görsel bir işaret, başka hiçbir mantığı (kilitleme, hesaplama vs.) etkilemiyor. Tek etkisi: `week.js`'te o günün başlığının yanına ✅ ekleniyor, böylece haftalık görünümden hangi günlerin bittiği anlaşılıyor.
- **Egzersiz durum işareti (2026-08-06 eklendi):** Her egzersiz instance'ında ✅ (istenildiği gibi yaptım) / 🔻 (yapamadım) üç-durumlu (boş↔good↔bad) hızlı işaret — `status` alanı, `updateInstanceStatus()`.
- **Basit şifre ekranı (2026-08-06 eklendi, 2026-08-07'de `js/auth.js` ile değiştirildi):** GitHub Pages public repo gerektiriyor (ücretsiz Pages için); kullanıcı ek bir engel istedi. `js/lock.js`: `localStorage`'da `gymbn_unlocked` bayrağı yoksa `app.js` gerçek uygulamayı hiç başlatmıyor, düz bir şifre ekranı gösteriyor. **Bilinçli olarak gerçek güvenlik değil** — statik site olduğu için şifre JS kaynağında görünür durumda, sayfa kaynağından aşılabilir; kullanıcıya bu açıkça söylendi. Asıl veri riski zaten yok: `gymbnData` (gerçek antrenman verisi) hiçbir zaman sunucuya gitmiyor, sadece kullanıcının kendi cihazında. **Firebase entegrasyonuyla birlikte `js/lock.js` silindi**, yerini gerçek Firebase email/şifre auth'u (`js/auth.js`) aldı — artık hem gerçek erişim kontrolü hem de senkron için gereken "hangi cihaz kimin verisi" kimliğini tek adımda çözüyor.
- **Çoklu-cihaz senkron: Firebase (2026-08-07 karar verildi, kod yazıldı):** Kullanıcı PC'de import ettiği veriyi telefonda görmeyince ("her cihaz kendi localStorage'ı" — beklenen davranış, açıklandı) gerçek senkron istedi. Önce "git'te tutsak olmaz mı" diye sordu — bu çözmez: tarayıcıdan git'e YAZMAK için ya tehlikeli bir GitHub yazma-token'ı client-side'da durması gerekir ya da bir backend (ki en baştan bilerek kaçınılan şey buydu), üstelik git push sonrası deploy gecikmesi "anlık güncel görme" isteğiyle de çelişir. Doğru araç: **Firebase** — client-taraflı okuma/yazmaya izin veren küçük bir bulut veritabanı, veri miktarı (tek kullanıcı, küçük JSON) ücretsiz kotanın çok altında.
  - **Mimari (Plan-agent'la doğrulandı, kaynaklar plan dosyasında):** Mevcut ~10 view dosyası (dashboard.js, week.js, dayEntry.js, vs.) hiç değişmedi — hepsi hâlâ `storage.js`'in senkron API'sini çağırıyor, bulut olduğunu bilmiyor bile. Tüm Firebase mantığı iki yeni, izole dosyada: `js/cloudSync.js` (Firebase init — CDN modüler SDK v12.17.1, `initializeFirestore`+`persistentLocalCache`+`persistentMultipleTabManager` ile **açıkça** offline persistence, tek doküman `users/{uid}/data/main`) ve `js/auth.js` (`js/lock.js`'in yerine geçti — sahte şifre yerine gerçek Firebase email/şifre girişi, **tek paylaşılan hesap** hem PC hem telefon için).
  - **Senkron akışı:** `storage.js`'in `saveState()`'i her çağrıldığında `state.updatedAt = Date.now()` damgalanıp `cloudSync.scheduleCloudPush()` tetikleniyor (kendi içinde ayrı ~1.5sn debounce — `bulkAdd.js`'in tek "Onayla"da 25-30+ `saveState` çağırdığı biliniyor, tek bulut yazmasına toplanıyor). Açılışta `loadState()` → `pullRemoteIfNewer()` (Firestore'daki `updatedAt` yerelden yeniyse `localStorage`'ı üzerine yazıp `location.reload()`) → `initApp()`. **Boş-durum güvenlik önlemi:** iOS Safari'nin 7+ gün açılmayan PWA'ların yerel verisini silebilmesi riskine karşı (`ITP`), ilk pull denemesi bitene kadar (`pullCompleted` bayrağı) hiçbir bulut yazması yapılmıyor — boş veri gerçek veriyi ezemiyor.
  - **`service-worker.js` zorunlu düzeltme:** `fetch` handler'a origin kontrolü eklendi (`url.origin !== self.location.origin` ise dokunma) — yoksa Firestore/Auth'a giden cross-origin istekler cache-first mantığına takılıp bozulabilirdi (gerçek bir Firebase SDK issue'suyla doğrulandı). `CACHE_NAME` v13'e çıkarıldı.
  - **Auth ekranı UX değişikliği:** Eski `lock.js` tüm `document.body`'yi değiştirip başarıda `location.reload()` yapıyordu. Yeni `auth.js` bunun yerine sadece `#view-root`'un içeriğini değiştiriyor (header/bottom-nav'ı `body.auth-gate` CSS class'ıyla gizliyor) — router'ın zaten kullandığı aynı container, "giriş yapınca hangi DOM'a initApp() çağıracağız" sorununu ortadan kaldırıyor, sayfa yenilenmeden akıcı geçiş sağlıyor.
  - **Bootstrap (veri kaybı riski):** Kullanıcının PC'de ve telefonda ŞU AN birbirinden farklı gerçek veri var. "İlk açılan kazanır" mantığına güvenilmedi (yarış durumu) — bunun yerine kod hiçbir cihaza gitmeden önce, kullanıcının seçtiği "doğru" cihazdan `exportBackup()` ile alınan JSON, Firebase Console'dan elle `users/{uid}/data/main`'e (bir "timestamp" tipi `updatedAt` alanıyla) tohumlanacak. Eski şemadaki (henüz `updatedAt` alanı olmayan) yerel veri `loadState()`'te otomatik `updatedAt:0` (yani "en eski") sayılıyor — bu sayede tohumlanan doküman, hangi cihaz önce açılırsa açılsın, ilk açılışta doğru şekilde kazanıyor.
  - **`firebaseConfig` gerçek değerlerle dolduruldu** (proje `gymbn-33e8f`), Firestore/Auth/Rules kuruldu, bootstrap verisi tohumlandı, kod push edilip canlıya alındı — bkz. Değişiklik Geçmişi.
  - **Şifremi unuttum (2026-08-07 eklendi):** Kullanıcı ilk giriş denemesinde kendi oluşturduğu şifreyi hatırlamadı. `js/auth.js`'e giriş ekranına ikinci bir buton eklendi ("Şifremi unuttum") — `cloudSync.js`'in yeni `resetPassword()` fonksiyonu (`sendPasswordResetEmail`) ile e-posta alanındaki adrese Firebase'in kendi sıfırlama mailini gönderiyor; kullanıcı linke tıklayıp yeni şifreyi kendisi belirliyor. Boş e-postayla tıklanırsa uyarı gösteriyor; başarılıysa yeşil (`--success`) bir onay mesajı (`.auth-hint`), hatalıysa kırmızı hata mesajı gösteriyor — ikisi asla aynı anda görünmüyor. Ayrıca fark edilen ayrı bir eksik de giderildi: Firebase Authentication'ın "Authorised domains" listesinde `alimbn.github.io` yoktu (sadece `localhost` + varsayılan `firebaseapp.com`/`web.app`), Firebase Console'dan eklendi.
  - **Sıfırlama maili hiç gelmedi → hesap silinip yeniden oluşturuldu (2026-08-07, aynı gün):** Yukarıdaki "Şifremi unuttum" butonu ve Firebase Console'un kendi "Reset password" aksiyonu ikisi de "gönderildi" diye onayladı (HTTP 200) ama mail kullanıcının gelen kutusuna hiç ulaşmadı (spam dahil). Kök neden araştırılırken öğrenilen: Admin SDK'nın `generate_password_reset_link()`'i (`returnOobLink:true`) Cloud Shell'in **kullanıcı** kimlik bilgileriyle (`credentials.ApplicationDefault()`) çağrılınca link üretmiyor, backend sessizce `{kind, email}` dönüyor (`oobLink` alanı yok) — bu işlem gerçek bir servis hesabı (service account) kimliği gerektiriyor. Servis hesabını impersonate edip token almaya çalışırken Claude Code'un otomatik izin sınıflandırıcısı bu adımı engelledi (hassas bir yetki yükseltme işlemi olduğu için, doğru bir karardı) — o yoldan vazgeçildi. **Çözüm:** kullanıcının onayıyla eski hesap silindi, kullanıcı Firebase Console'dan (Add user) aynı email ile **kendi belirlediği yeni bir şifreyle** yeni bir hesap oluşturdu. Yeni hesabın UID'si eskisinden farklı olduğu için (Firebase UID'leri asla yeniden kullanmıyor), Firestore'daki `users/{eskiUid}/data/main` verisi Google Cloud Shell'de küçük bir Python betiğiyle `users/{yeniUid}/data/main`'e **kopyalandı** (eski doküman silinmedi, sadece artık kullanılmıyor — istenirse sonradan temizlenebilir). Ders: bu uygulamada bir Firebase Auth kullanıcısını silip yeniden oluşturmak, Firestore verisini de elle yeni UID'ye taşımayı gerektiriyor, aksi halde veri "kaybolmuş" gibi görünür (aslında sadece erişilemez, silinmemiş).
- **Haftalık Özet ekranı (2026-08-08 eklendi):** Kullanıcının amacı: hafta bitiminde hocaya "bu hafta durum neydi, gelişme var mı" diye göstermek — hoca notlara bakıp yeni haftayı ona göre planlayacak. `js/views/weekSummary.js` (yeni), rota `#/week/:mondayIso/summary`, `week.js`'te (o haftada en az bir gün kaydı varsa görünen) "📄 Haftalık Özet" linkinden açılıyor. İçerik: üstte 3 istatistik kutusu (gün sayısı, ✅ sayısı, 🔻 sayısı), altında her gün için gün tipi/no/tamamlandı işareti + her egzersiz için planlanan vs. yapılan (set bazlı, `weight×reps (rir N)` formatında) + not (varsa). **Bilinçli olarak "basit özet"** — kullanıcı açıkça "şimdi basit özet ile başlayalım, sonra iyileştirmeler yaparız" dedi, o yüzden grafik/trend/geçen-haftayla-kıyas gibi şeyler bu turda YOK, sadece o haftanın ham verisinin okunaklı bir sunumu.
  - **Düzeltme (aynı gün):** Kullanıcı canlıda test edince iki sorun bildirdi: (1) her gün-tipi bloğunun sütunları birbirine hizasız — sebep, her `<table>`'ın varsayılan `table-layout:auto` ile İÇERİĞİNE göre kendi sütun genişliklerini bağımsız hesaplaması (bir bloğun notu yoksa/kısaysa, diğer sütunlar rastgele genişliyordu). Çözüm: `table-layout:fixed` + her tabloda aynı `<colgroup>` yüzde genişlikleri (Egzersiz 28%, Ağırlık 13%, Set 7%, Tekrar 15%, Rir 9%, Not 28%) — artık tüm bloklarda sütunlar birebir hizalı. (2) Sayfa header'ı ("Gym Takip") ve alt navigasyon boşuna yer kaplıyordu — bu ekran zaten ekran görüntüsü almak için var, gezinme çubuğuna ihtiyaç yok. `util.js`'e `setAppChromeHidden()` eklendi (aynı `setViewportZoomable()` deseni: `router.js` her navigasyonda kapatıyor, bu ekran kendi açıyor) — `.app-header`/`.bottom-nav` gizleniyor, `#view-root` tüm yüksekliği kullanıyor. Sadece `weekSummaryDesktop.js`'de kullanılıyor, mobil kart görünümüne dokunulmadı.
  - **2×2 ızgara düzeni (aynı gün, ikinci düzeltme):** Kullanıcı "kare yapalım, 2 satır 2 sütun — ilk satırda anterior 1-2, ikinci satırda posterior 1-2" dedi — dikey tek sütun yerine, aynı "aile"den gün-tiplerini (adın sonundaki `-N` atılarak: "Anterior-1"/"Anterior-2" → "Anterior") yan yana aynı satıra koyan bir ızgara istedi. `groupByDayTypeFamily()` eklendi: günleri temel gün-tipi adına göre gruplar, grup sırası haftada İLK GÖRÜLDÜĞÜ sıraya göre belirlenir (kullanıcının verisinde bu otomatik olarak Anterior satırının önce, Posterior satırının sonra çıkmasını sağlıyor — "Anterior"/"Posterior" hiçbir yerde hardcode edilmedi, sadece sondaki sayı/`-N` deseni kırpılıyor, böylece kullanıcı gün-tipi adlarını değiştirse veya farklı bir isimlendirme kullansa da aynı mantık çalışmaya devam eder). Her grup `.desktop-day-row` (flex, `flex-wrap`) içinde yan yana diziliyor; masaüstü genişlikte (≥ ~1260px, iki blok + boşluk) yan yana, dar ekranda otomatik alt alta düşüyor.
  - **Masaüstü/tablo görünümü (2026-08-08, aynı gün eklendi):** Kullanıcı hocasının kullandığı Excel formatının bir ekran görüntüsünü gösterdi — her gün-tipi bloğu için satır satır egzersiz, sütun sütun ağırlık/set/tekrar/rir, birden fazla setin değeri farklıysa hücrede `#` ile ayrılmış ("8 # 7", "11 # 10 # 08"). Kullanıcının isteği net: **mevcut mobil kart görünümünü bozma, ayrı bir buton ekle.** Bu yüzden `js/views/weekSummary.js`'e hiç dokunulmadı; tamamen ayrı, yeni bir dosya (`js/views/weekSummaryDesktop.js`) ve yeni bir rota (`#/week/:mondayIso/summary-desktop`) yazıldı, `week.js`'e ikinci bir link ("🖥️ Masaüstü Özeti") eklendi. Tablo mantığı: her egzersiz satırında ad+planlanan (küçük alt satır), sonra Ağırlık/Set/Tekrar/Rir sütunları — `joinSetValues()` set'ler arasında değer aynıysa tek gösteriyor, farklıysa Excel'deki gibi ` # ` ile birleştiriyor. Bu görünüm de aynı `setViewportZoomable(true)` mekanizmasını kullanıyor (bkz. aşağıdaki madde) — geniş tablo mobilde yatay scroll + pinch-zoom ile okunabiliyor.
  - **Zoom/geniş-ekran isteği çözümü:** Kullanıcı "basınca daha geniş bir ekran çıkarabilirsin, pinch ile zoom yapılıp bakılır" dedi. Uygulamanın `index.html` viewport meta etiketi `maximum-scale=1` ile pinch-zoom'u SİTE GENELİNDE kapatıyor (antrenman girişi sırasında yanlışlıkla zoom olmasın diye, bilinçli bir karardı). Bunu tek bu ekran için gevşetmek üzere `util.js`'e `setViewportZoomable(bool)` eklendi; `router.js` her navigasyonda önce kilitli hale döndürüyor (`setViewportZoomable(false)`), `weekSummary.js` kendi `render()`'ında `setViewportZoomable(true)` çağırıyor. Böylece SADECE özet ekranındayken pinch-zoom açık, başka herhangi bir ekrana geçildiğinde otomatik tekrar kapanıyor — başka hiçbir view'a dokunmadan.
- **Özel domain (2026-08-07 karar verildi, henüz uygulanmadı):** Kullanıcı ayrıca kendi domain'ini almak istiyor. Netleştirilen nokta: domain senkron sorununu ÇÖZMEZ (sadece link kozmetiği, backend/senkron kapasitesi eklemez) — **ama kullanıcı yine de istiyor, bu iki karar birbirinden bağımsız, biri diğerinin yerine geçmiyor.** Domain'i kullanıcı kendi satın alacak (ödeme işlemi yapamam). Alındıktan sonra GitHub Pages'e bağlamak teknik olarak basit: repo'ya `CNAME` dosyası + domain sağlayıcısında DNS (A/CNAME) kaydı.
- **Gün girişi ekranında 5 iyileştirme (2026-08-08 eklendi):** Kullanıcı gerçek kullanımdan sonra "Yapılan" (actual) set girişini iyileştirmek istedi. Netleştirilen çelişki: "tekrar" sayısal-only olunca "75sn"/"tükenene kadar" gibi gerçek verinde geçen serbest ifadeler actual tarafında girilemez olurdu — kullanıcı **"sadece sayı gir"** dedi (süre bazlı egzersizler için "75sn" yerine "75" girilecek, birim etiketi zaten planlanan tarafında yazıyor). Uygulanan değişiklikler:
  1. `js/components/setRows.js`'te "Tekrar" ve "Rir" artık **native `<select>`** (0-30 ve 0-9 arası) — serbest metin/stepper değil. iPhone'da otomatik kaydırmalı çark (wheel picker) olarak açılıyor (kullanıcının attığı ekran görüntüsündeki gibi), Android/masaüstünde normal açılır liste. Ağırlık alanı DEĞİŞMEDİ — hâlâ serbest metin + stepper (dual-dumbbell "15x15kg" gibi notasyonları desteklemesi gerektiği için).
  2. `.set-row-bottom` düzeni: Tekrar `flex:3`, Rir `flex:1` — rir tek haneli olduğu için dar, tekrar çift haneli olabildiği için geniş.
  3. `storage.js`: `buildActualSetsFromPrescribed()` artık prescribed'daki serbest metinden (örn. "8-9", "75sn") İLK SAYIYI çıkarıp (`extractLeadingInt`) temiz bir tekrar/rir değeriyle actual set'leri dolduruyor; rir ayrıca 0-9'a sıkıştırılıyor (`clampRir`). `addActualSet()`'in prescribed-fallback dalı da aynı mantığı kullanıyor.
  4. **Not (geriye dönük veri):** Bu değişiklikten ÖNCE kaydedilmiş, tekrar/rir alanında sayısal olmayan değer içeren eski kayıtlar (örn. geçmişteki "75sn" actual girişleri) yeni select'te seçili görünmeyebilir (tarayıcı varsayılan olarak ilk seçeneği/boş gösterir) — ancak veritabanındaki GERÇEK değer silinmez/değişmez, sadece o alana dokunulmadığı sürece görüntüleme farkı olur. Bilinçli olarak eski veri için bir göç (migration) yazılmadı, kapsam dışı bırakıldı.
  5. `js/views/dayEntry.js`: "Nasıl gitti?" (✅/🔻) butonuna basınca (durum TEMİZLENMİYORSA, yani gerçekten bir durum SEÇİLİYORSA) o akordiyon kapanıp **bir sonraki egzersiz otomatik açılıyor**, görünüme kaydırılıyor (`scrollIntoView`) — antrenman sırasında elle kapat/aç yapmadan sırayla ilerlemek için. Son egzersizse sadece kapanıyor.
  6. `.prescribed-block`'a ince `var(--warning)` (amber) çerçeve eklendi — "Planlanan (Hoca)" alanının hocanın alanı olduğunu görsel olarak ayırt etmek için. **Bilinçli olarak input'lar hâlâ düzenlenebilir** — kullanıcı şu an hocanın programındaki hataları/eksikleri elle düzeltmek zorunda kalıyor. Kullanıcının notu: **ileride bu alan tamamen salt-okunur/kilitli yapılacak** (henüz plan yok, sadece niyet kaydı).
  7. `storage.js`: `addExerciseInstance()`'ın geçmişsiz varsayılanında `rir: ''` → `rir: '0'`.
  8. **Durum rozetleri (aynı gün, ikinci tur):** Kullanıcı özet ekranındaki kırmızı "yapılamadı" göstergesinin çerçevesiz/düz emoji göründüğünü fark edip tutarlı bir rozet istedi: kırmızı zemin + beyaz aşağı ok (▼), ve daha önce hiç görünmeyen "henüz işaretlenmedi" durumu için turuncu zemin + beyaz tire (−). `util.js`'e paylaşılan `statusBadge(status)` eklendi (yeşil ✓ / kırmızı ▼ / turuncu − — üç durum da artık dairesel, aynı boyutta rozetler), `js/views/dayEntry.js` (kart başlığı), `js/views/weekSummary.js`, `js/views/weekSummaryDesktop.js` üçü de bu tek fonksiyonu kullanacak şekilde güncellendi — artık her egzersiz, işaretlenmemiş olsa bile, bir durum rozeti gösteriyor. "Nasıl gitti?" butonlarının kendisi (aktif/pasif dolgu rengiyle çalışan ayrı bir stil) değiştirilmedi, sadece pasif GÖSTERGE rozetleri.
- **Dinlenme kronometresi / floating timer (2026-08-08 eklendi):** Kullanıcı setler arası dinlenmeyi telefonun kendi saatiyle tutuyordu, uygulama içinden çıkmak zorunda kalmadan takip etmek istedi. Tasarım kararları kullanıcıya soruldu ve onaylandı: **tek dokunuş duraklatır/devam ettirir** (sıfırlamaz — kaldığı değerden devam eder, gerçek kronometre mantığı), **uzun basış (600ms) sıfırlar**, **sürüklenebilir** (diğer butonları kapatırsa taşınabilir), ve **hiçbir ekranda kaybolmaz** (kullanıcının netleştirdiği nokta — başta "sadece gün girişi ekranında" önerilmişti, kullanıcı "kaybolmaması da uygun" diyerek tüm uygulama genelinde kalıcı olmasını istedi).
  - `js/components/restTimer.js` (yeni) — `document.body`'ye DOĞRUDAN ekleniyor, `#view-root`'un DIŞINDA duruyor; `router.js`'in `renderRoute()`'u her navigasyonda sadece `#view-root.innerHTML`'i temizlediği için (bkz. `router.js`) bu buton hiçbir zaman silinmiyor — hafta görünümünden geçmişe, ödemelere her yere geçilse de aynı buton aynı yerde (sürüklenmişse o konumda) kalmaya devam ediyor.
  - Üç jest TEK pointer olayı zinciriyle (`pointerdown`/`pointermove`/`pointerup`) ayırt ediliyor: 8px'i aşan hareket = sürükleme (dokunma/başlatma tetiklenmiyor), 600ms dolmadan bırakılan ve hareket etmeyen dokunuş = başlat/duraklat, 600ms'i aşan basılı tutma = sıfırlama (bırakılmadan, basılıyken tetikleniyor).
  - `app.js`'te `initApp()` içinde bir kez çağrılıyor (login sonrası, route'lar kurulduktan sonra) — auth ekranında hiç görünmüyor. `body.chrome-hidden` (masaüstü özeti tam ekran modu) aktifken de CSS ile gizleniyor, ekran görüntüsüne karışmasın diye.
  - Süre sadece bellekte tutuluyor (localStorage'a yazılmıyor) — sayfa yenilenirse veya uygulama yeniden başlatılırsa sıfırlanır. Bilinçli olarak basit tutuldu, kullanıcı bunu istemedi.
  - Gerçek `pointerdown`/`pointermove`/`pointerup` olayları simüle edilerek (tıklama değil — bu buton `click` dinlemiyor, saf pointer tabanlı) yerelde hassas şekilde doğrulandı: 3 saniyelik çalışma → "00:03", duraklat → sabit kalıyor, devam ettir → kaldığı yerden devam ediyor ("00:03" + 2sn = "00:05"), 700ms basılı tutma → "00:00"'a sıfırlanıyor, büyük bir sürükleme hareketi → konum değişiyor AMA başlat/duraklat tetiklenmiyor, `#view-root` içeriği tamamen değiştirildiğinde (navigasyon simülasyonu) buton DOM'da kalmaya devam ediyor — hepsi beklenen sonuçları verdi.
- **Yerel geliştirme sunucusu:** Düz `python -m http.server` yerine `devserver.py` (bu projede) kullanılıyor — sebebi, tarayıcının `<script type="module">` dosyalarını agresif cache'lemesi (özellikle hash-routing'de sadece `#...` değişen navigasyonlarda tam sayfa yenilenmediği için eski JS çalışmaya devam edebiliyordu). `devserver.py` tüm yanıtlara `Cache-Control: no-store` ekliyor, bu sorunu çözüyor.
- Detaylı veri şeması, dosya yapısı ve ekran tasarımları için bkz. plan dosyası: `C:\Users\mubin\.claude\plans\vivid-squishing-floyd.md`

## Sonraki Adımlar
- [x] Gereksinim toplama + mimari kararlar (kullanıcıyla netleşti)
- [x] Uygulama planı yazıldı ve onaylandı
- [x] İskelet (index.html, style.css, storage.js, router) — yazıldı ve test edildi
- [x] Egzersiz / gün-tipi kütüphaneleri — yazıldı, ekle/yeniden adlandır/arşivle test edildi
- [x] Gün girişi ekranı (set bazlı giriş, carry-forward, prescribed↔actual canlı senkron, son-sefer kıyası) — yazıldı, tarayıcıda uçtan uca test edildi
- [x] Dashboard, ödemeler, geçmiş, diğer/yedekleme ekranları — yazıldı, temel akışlar test edildi (export/import fonksiyon seviyesinde test edildi)
- [x] PWA kabuğu (manifest, service worker, placeholder ikonlar) — dosyalar yazıldı, app.js'e SW kaydı eklendi
- [x] **Son kapsamlı uçtan uca test** — gerçekçi bir haftalık senaryo (8 egzersiz, gün girişi, ödeme, geçmiş, kütüphane düzenleme) baştan sona test edildi; service worker offline davranışı sunucuyu kapatıp doğrulandı (uygulama önbellekten sorunsuz açıldı); manifest + ikonlar doğrulandı; konsol boyunca sıfır hata. Test verileri temizlendi, uygulama şu an tertemiz/boş durumda.
- [x] Placeholder ikonlar (basit dambıl motifi) — istenirse daha sonra değiştirilebilir
- [x] Haftalık program görünümü (`#/week`) — giriş noktası hafta merkezli oldu, uçtan uca test edildi
- [x] Toplu program yapıştırma + önizleme (`#/bulk-add`) + egzersiz durum işareti (✅/🔻) — gerçek kullanıcı metniyle test edildi
- [x] Gün/hafta silme, egzersiz akordiyonu, gün tamamlama işareti — hepsi test edildi
- [x] Basit şifre ekranı (`js/lock.js`) — gerçek şifre kullanıcıdan alındı, canlıda test edildi
- [x] **GitHub Pages deploy** — repo `github.com/alimbn/gymbn` (public), push GitHub Desktop ile yapıldı, Pages `main` / `(root)`'tan yayınlanıyor. Canlı site doğrulandı: şifre ekranı çalışıyor, service worker scope doğru (`https://alimbn.github.io/gymbn/`), manifest/ikonlar 200 dönüyor, cache doluyor (24 dosya), hafta görünümüne navigasyon çalışıyor, konsolda sıfır hata.
- [x] Firebase senkron + gerçek auth kodu yazıldı (`js/cloudSync.js`, `js/auth.js`, `storage.js`/`app.js`/`service-worker.js` entegrasyonu) — yerelde config'siz duman testi yapıldı
- [x] Kullanıcıdan `firebaseConfig` alındı, koda işlendi
- [x] Firebase Console: Firestore (eur3, Native mode) oluşturuldu, Security Rules yayınlandı (`request.auth.uid == userId`), Authentication → Email/Password açıldı, kullanıcı kendi hesabını kendi oluşturdu
- [x] Bootstrap: kullanıcı telefondan yedek attı, Google Cloud Shell üzerinden (kullanıcının kendi yetkili Google hesabıyla, Firestore Admin SDK ile) `users/{uid}/data/main` dokümanına tohumlandı, Firestore Console'dan doğrulandı
- [x] Push edildi (`e7040e0`) + GitHub Pages deploy doğrulandı + canlı sitede login ekranı/SW v13/konsol hatasız doğrulandı
- [x] Kullanıcı PC'de ve telefonda gerçek şifreyle giriş yapıp iki cihazda da aynı veriyi gördüğünü doğruladı (ekran görüntüsüyle teyit edildi, 2026-08-08)
- [x] Sıfırlama maili sorunu çözüldü: hesap silinip yeniden oluşturuldu, Firestore verisi yeni UID'ye taşındı, kalıcı "Şifremi unuttum" akışı eklendi
- [x] Haftalık Özet ekranı (`#/week/:mondayIso/summary`) — basit v1, yerel test harness'ıyla doğrulandı
- [x] Masaüstü/tablo özeti (`#/week/:mondayIso/summary-desktop`) — Excel formatına benzer, ayrı buton, yerel test harness'ıyla doğrulandı (mobil + masaüstü genişlik)
- [x] Gün girişi: tekrar/rir için sayısal `<select>`, alan genişlikleri, auto-advance akordiyon, planlanan blok çerçevesi, rir varsayılanı 0 — hepsi yerel test harness'ıyla doğrulandı
- [x] Durum rozetleri (`statusBadge()`: yeşil ✓ / kırmızı ▼ / turuncu −) — dayEntry/weekSummary/weekSummaryDesktop'ta tutarlı, yerelde doğrulandı
- [x] Dinlenme kronometresi (`js/components/restTimer.js`) — sürüklenebilir, duraklat/devam ettir, uzun basışla sıfırlama, tüm ekranlarda kalıcı — gerçek pointer olayları simüle edilerek hassas şekilde doğrulandı
- [ ] Haftalık Özet / Masaüstü Özeti için kullanıcıdan gelecek iyileştirme istekleri (henüz belirtilmedi)
- [ ] **"Planlanan (Hoca)" bloğunu salt-okunur/kilitli yapmak** (kullanıcının notu: "burayı daha sonra pasif yapacağız, müdahale edilmez yapacağız" — şimdilik sadece görsel olarak (amber çerçeve) ayırt ediliyor, hâlâ düzenlenebilir; ne zaman/nasıl kilitleneceği henüz belirsiz, muhtemelen hoca kendi ekranından girer hale gelince anlamlı olacak)
- [ ] Özel domain satın alma + GitHub Pages'e bağlama (kullanıcı domain'i kendi alacak)
- [x] Admin + Hoca Ekranları Faz A — kod yazıldı, `firestore.rules` Firebase Emulator Suite ile 28/28 test PASS, `assignProgram.js`'in program-atama mantığı gerçek kullanıcı metniyle 15/15 test PASS, yeni sayfalar (admin.html/coach.html/join.html) yerelde konsol hatasız açılıyor — bkz. "## Admin + Hoca Ekranları, Faz A"
- [ ] Admin + Hoca Faz A'nın CANLIYA ALINMASI — admin hesabı, `admins/{uid}` dokümanı, kural karşılaştırma+publish ve kod push'u **tamamlandı (2026-08-15)**; kalan tek adım: kullanıcı kendi admin hesabıyla `admin.html`'e giriş yapıp hoca daveti oluşturacak, gerçek hoca `join.html` ile kabul edecek, öğrenci daveti→kabul→program atama akışı canlıda uçtan uca denenecek — bkz. "## Admin + Hoca Ekranları, Faz A"
- [ ] Hoca Faz B (antrenman raporlarını görme) ve Faz C (ödeme takibi) — şema/kurallar hazır, ekranlar henüz yazılmadı

## Kapsam Dışı (şimdilik)
Grafik/analitik (son-sefer kıyası dışında), gerçek zamanlı çoklu-kullanıcı özellikleri (uygulama tek paylaşılan hesapla kullanılıyor, çoklu hesap/rol sistemi yok).

## Canlı Link
`https://alimbn.github.io/gymbn/` — GitHub Pages, `alimbn/gymbn` reposunun `main` branch'inden yayınlanıyor. Şifre ekranı arkasında (bkz. Kararlar → Basit şifre ekranı). Güncelleme: bir sonraki `git push`'tan birkaç dakika sonra otomatik yayına giriyor; kullanıcının tarayıcısı da SW'nin otomatik güncelleme mekanizması sayesinde kendini tazeliyor (manuel önbellek temizliği gerekmiyor).

## Nasıl Çalıştırılır (yerel geliştirme)
Proje klasöründe: `python devserver.py 5174 .` → `http://localhost:5174`
(Claude Code içinde `.claude/launch.json`'daki `gymbn-static` konfigürasyonu bunu otomatik yapar.)

Yayına almak için: `git add -A && git commit -m "..."` sonra `git push` (bu ortamdan internet erişimi yok — push'u kullanıcı kendi GitHub Desktop'ından ya da terminalinden yapıyor, bkz. Bilinen Notlar).

## Bilinen Notlar
- **Terminal internet erişimi tutarsız/oturuma göre değişiyor:** 2026-08-05/06 oturumlarında Bash/PowerShell'den `github.com`'a DNS çözümlemesi bile başarısız oluyordu, `git push` hep kullanıcının kendi GitHub Desktop'ından yapılıyordu. 2026-08-07'de (bu oturum, aynı gün Firebase işi sırasında) `git push` **doğrudan bu ortamdan başarıyla çalıştı** — önceki kısıt bu oturumda geçerli değildi. Sonuç: her oturumun başında `git push` önce doğrudan denenmeli, sadece başarısız olursa kullanıcıdan GitHub Desktop ile yapması istenmeli — "internet yok" varsayımını körü körüne tekrar etmemeli.
- Tarayıcı önizleme araçlarının (hem Browser pane hem Claude in Chrome) internet erişimi her zaman var — deploy sonrası canlı site doğrulaması bu yüzden bu araçlarla her zaman yapılabiliyor.
- **Telefon/LAN erişimi (`192.168.x.x:5174`) bu geliştirme ortamından çalışmıyor** — sadece `localhost` erişilebilir (test edildi: `Test-NetConnection` LAN IP'sine bağlanamadı). Telefonda gerçek kurulum için GitHub Pages deploy'u gerekiyor.
- **Service worker otomatik güncelleme (2026-08-06 eklendi):** Önceden, kod güncellendiğinde kullanıcının tarayıcısı eski önbelleklenmiş sürümü göstermeye devam edebiliyordu (gerçek bir kullanıcı bunu yaşadı — "Programı Yapıştır" butonu görünmüyordu). Artık `service-worker.js`'in `activate` handler'ı gerçek bir güncelleme olduğunu (eski cache adı varlığından) anlayıp açık sekmeleri otomatik yeniliyor — kullanıcının elle önbellek temizlemesi gerekmiyor. Bu mekanizma bizzat test edildi (eski sürüm simüle edilip yeni sürüme geçişte sekmenin kendiliğinden yenilendiği doğrulandı). Mevcut takılı kalmış bir tarayıcıyı bu düzeltmeyle kurtarmak için yine de bir kerelik elle müdahale gerekiyor (SW unregister + cache temizleme, localStorage'a dokunmadan).

## Değişiklik Geçmişi
- 2026-08-04: Proje başlatıldı, plan onaylandı, bu dosya oluşturuldu. Aynı gün içinde MVP'nin tüm ekranları yazıldı, PWA kabuğu eklendi, kapsamlı uçtan uca test (offline dahil) geçildi. MVP kullanıma hazır.
- 2026-08-06 (devam): Kullanıcı gerçek kullanımda "Programı Yapıştır" butonunu göremedi — eski service worker sürümü tarayıcısında takılı kalmıştı (LAN IP üzerinden telefon erişimi de bu ortamda çalışmadığı ayrıca tespit edildi). Kök nedeni çözmek için service worker'a otomatik güncelleme/yenileme mekanizması eklendi (CACHE_NAME v4), test edildi.
- 2026-08-06 (devam): Gün/hafta silme, egzersiz akordiyonu, gün tamamlama işareti, basit şifre ekranı eklendi — hepsi test edildi. Ardından **GitHub Pages'e deploy edildi**: yerel git repo hazırlandı (3 commit), kullanıcı `github.com/alimbn/gymbn` reposunu oluşturdu, push'u GitHub Desktop ile kendisi yaptı (bu ortamdan `git push` denendi ama internet erişimi olmadığı ortaya çıktı — `github.com` DNS çözümlemesi bile başarısız oluyor). Pages ayarlandıktan sonra canlı site (`https://alimbn.github.io/gymbn/`) tarayıcı önizleme aracıyla uçtan uca doğrulandı: şifre ekranı, SW scope, manifest/ikonlar, cache, navigasyon — hepsi sorunsuz. Kullanıcı bugün salonda saha testine başlıyor.
- 2026-08-05: Haftalık program görünümü eklendi (`js/views/week.js`) — giriş noktası günden haftaya taşındı. Dashboard CTA, `dayEntry.js` (sadeleşti, geri butonu `history.back()`'e geçti), `app.js` rotaları, nav linki güncellendi. Bu sırada bulunup düzeltilen 2 hata: (1) `app.js`'te `history` modül import'u ile global `window.history` arasında isim çakışması, (2) `dayEntry.js`'in geri butonunun her zaman panele dönmesi (artık hafta görünümüne de dönebilmesi lazımdı). Uçtan uca test edildi, test verisi temizlendi.
- 2026-08-06: Toplu program yapıştırma (`js/bulkParse.js` + `js/views/bulkAdd.js`) ve egzersiz durum işareti (✅/🔻) eklendi. Parser, kullanıcının gerçek 4 günlük/32 satırlık hoca mesajına karşı hem Node.js'te hem tarayıcıda test edildi — tüm alanlar (ağırlık, set, tekrar, rir, not, "(15x15kg)" gibi ağırlık açıklamaları, "rir X olana kadar tekrar"/"tükenene kadar" gibi serbest ifadeler) doğru ayrıştı. Aynı programın ikinci kez yapıştırılmasında egzersiz/gün-tipi kütüphanesinin doğru eşleştiği (kopya oluşturmadığı) doğrulandı. `service-worker.js` cache'i v3'e yükseltildi. Test verisi temizlendi.
- 2026-08-07: Kullanıcı saha testinde PC↔telefon senkron olmadığını fark etti (beklenen davranış). "Git'te tutmak" fikri değerlendirilip senkronu çözmediği netleştirildi. Kullanıcı **Firebase ile gerçek senkron** yapmaya karar verdi. "Yeni domain almak" fikri de gündeme geldi — domain'in senkronu tek başına çözmediği netleştirildi ama kullanıcı domain'den vazgeçmedi, **her ikisini de** istiyor (ilk özetimde bunu yanlış anlayıp "domain fikri terk edildi" diye yazmıştım, kullanıcı düzeltti — düzeltildi). "Yarın devam edeceğiz" dedi — bu oturumda kod yazılmadı, sadece karar + gerekçe bu dosyaya ve kalıcı hafızaya kaydedildi.
- 2026-08-07 (devam, aynı gün): Kullanıcı "evet ne yapıyoruz şimdi" diyerek Firebase işine hemen başlanmasını istedi (bir gün beklemeden). Plan Mode'dan geçildi, bir Plan-agent doğrulama turu (CDN modüler SDK, offline persistence API'si, service worker/Firestore çakışma riski, doküman boyutu, Auth kalıcılığı, iOS depolama tahliyesi, bulk-add yazma patlaması, bootstrap yarışı) sonrası plan onaylandı ve uygulandı: `js/cloudSync.js` + `js/auth.js` yazıldı, `js/lock.js` silindi, `storage.js`'e `updatedAt` alanı + `scheduleCloudPush` bağlandı, `app.js` async auth-gated boot'a geçti, `service-worker.js`'e zorunlu origin-kontrolü fix'i + `CACHE_NAME` v13 eklendi, `style.css`'e giriş ekranı stilleri eklendi (bu arada fark edilen küçük bir eksik de düzeltildi: `input[type=password]` hiç ortak input stiline dahil değilmiş, artık `email`/`password` ikisi de dahil).
- 2026-08-07 (devam, aynı gün): Kullanıcı Firebase Console'da projeyi (`gymbn-33e8f`) oluşturup web app config'ini paylaştı; bu, Claude in Chrome bağlantısı üzerinden (kullanıcının kendi gerçek, giriş yapılmış Chrome'u) canlı olarak takip edilip yürütüldü — Firestore veritabanı (eur3, Native mode, Standard edition) oluşturuldu, Security Rules `request.auth != null && request.auth.uid == userId` olarak yayınlandı, Authentication → Email/Password açıldı. Paylaşılan giriş hesabını kullanıcı **kendisi** oluşturdu (şifre girme/hesap oluşturma işlemi bilinçli olarak asistana bırakılmadı — sadece kullanıcının kendisinin yapması gereken bir adım). Kullanıcı telefondan güncel yedeği (`gymbn-yedek-2026-08-07.json`, 4 gün/28 egzersiz kaydı) gönderdi; bootstrap tohumlaması Firestore Console'un elle alan-alan arayüzü yerine (yüzlerce iç içe alan olduğu için pratik değildi) **Google Cloud Shell** üzerinden (kullanıcının aynı Google hesabıyla, gerçek internet erişimiyle) küçük bir Python + `google-cloud-firestore` betiğiyle tek seferde `users/{uid}/data/main`'e yazıldı (`updatedAt` gerçek Firestore server timestamp), sonra geçici dosyalar Cloud Shell'den silindi. `firebaseConfig` `js/cloudSync.js`'e işlendi, değişiklikler commit edilip (`e7040e0`) **başarıyla push edildi** (bu oturumda ortamın internet erişimi olduğu görüldü — önceki oturumlardaki kısıt artık geçerli değil, bkz. Bilinen Notlar). GitHub Pages deploy'u doğrulandı, canlı site login ekranını hatasız gösteriyor, service worker v13'e güncellendi. Kalan tek adım: kullanıcının PC ve telefonda gerçek şifreyle giriş yapıp senkronu kendi gözüyle doğrulaması.
- 2026-08-07 (devam, aynı gün): Kullanıcı ilk giriş denemesinde "mailim ve şifremi hatırlamıyorum" dedi. Firebase Console'dan tek seferlik bir şifre sıfırlama maili gönderildi (Authentication → Users → Reset password) kullanıcının sorununu hemen çözmek için; ardından kullanıcı "bizim arayüze şifre sıfırlama butonu ekleyebilir misin" diye sordu, bu yüzden `js/auth.js` + `js/cloudSync.js`'e kalıcı bir "Şifremi unuttum" akışı eklendi (bkz. Kararlar). Ayrıca teşhis sırasında fark edilen `alimbn.github.io`'nun Firebase'in yetkili domain listesinde eksik olduğu düzeltildi (OAuth girişleri için gerekli, email/şifre için doğrudan etkisi yoktu ama düzeltilmesi doğruydu). Yerelde (gerçek Firebase config'iyle) test edildi: boş e-postayla tıklama uyarı veriyor, geçerli e-postayla tıklama başarı mesajı gösteriyor, konsolda hata yok.
- 2026-08-07 (devam, aynı gün): Sıfırlama maili tekrar denendi (Console "Reset password" tekrar tetiklendi, "email has been sent" onayı alındı) ama yine ulaşmadı. Kök neden araştırması Google Cloud Shell üzerinden yapıldı: Admin SDK `generate_password_reset_link()` çağrısı 200 dönüyor ama `oobLink` alanı boş — sebebi, işlemin gerçek bir servis hesabı kimliği istemesi, Cloud Shell'in varsayılan kullanıcı kimlik bilgileriyle (ADC) çalışmıyor. Servis hesabını impersonate edip token almaya çalışırken Claude Code'un izin sınıflandırıcısı bu adımı (hassas bir yetki yükseltme işlemi olduğu için) engelledi — bu sınıra saygı gösterilip o yoldan vazgeçildi. Kullanıcının onayıyla en pratik çözüme gidildi: eski hesap silindi, kullanıcı yeni bir hesabı (aynı email, kendi belirlediği yeni şifre) kendisi oluşturdu, Firestore'daki antrenman verisi Cloud Shell'de küçük bir Python betiğiyle eski UID'den yeni UID'ye kopyalandı (doğrulandı). Belgelendi (PROJECT.md).
- 2026-08-08: Kullanıcı yeni hesapla giriş yapıp senkronu kendi gözüyle doğruladı (ekran görüntüsüyle: haftalık program hem PC hem telefonda aynı görünüyor). Yeni istek: hafta sonunda hocaya gösterilecek bir "Haftalık Özet" ekranı — `js/views/weekSummary.js` yazıldı (bkz. Kararlar), `#/week/:mondayIso/summary` rotası, `week.js`'e giriş linki eklendi. Kullanıcının "pinch ile zoom" isteği için `util.js`'e `setViewportZoomable()` eklenip `router.js`'in her navigasyonda sıfırlaması sağlandı — sadece bu ekranda pinch-zoom açık. Yerel bir test harness'ıyla (auth gerektirmeden, doğrudan modül import ederek) sahte veriyle render doğrulandı: istatistik kutuları, planlanan/yapılan karşılaştırması, durum ikonları, notlar, viewport toggle — hepsi doğru çalıştı. Kullanıcı ayrıca uzun vadeli bir yön belirtti (hocaya ekran, sonra çoklu-hoca/öğrenci platformu) — henüz plan yok, sadece "Gelecek Vizyonu" bölümüne kayıt düşüldü. Kullanıcı "basit özet, sonra iyileştirmeler yaparız" dedi — bu turda grafik/trend YOK, sıradaki iyileştirmeler bir dahaki istekte netleşecek.
- 2026-08-08 (devam, aynı gün): Kullanıcı hocasının Excel formatının ekran görüntüsünü gösterip "masaüstü görünümü de yapabilir misin, mevcutu bozma, ekstra bir buton ekle" dedi. `js/views/weekSummaryDesktop.js` (yeni, mevcut `weekSummary.js`'e hiç dokunulmadı) + `#/week/:mondayIso/summary-desktop` rotası + `week.js`'e ikinci link ("🖥️ Masaüstü Özeti") eklendi. Excel'deki set-bazlı `#`-ayraçlı hücre formatı (`"8 # 7"`, `"11 # 10 # 08"`) birebir taklit edildi (`joinSetValues()` — değerler aynıysa tek gösterir, farklıysa `#` ile birleştirir); aynı `setViewportZoomable()` mekanizması bu ekranda da kullanıldı. Yerel test harness'ıyla (mobil + masaüstü genişlik) doğrulandı, geniş tablo mobilde beklendiği gibi yatay scroll oluyor.
- 2026-08-08 (devam, aynı gün): Kullanıcı canlıda test edip iki sorun bildirdi (sütun hizası + boşa giden header/nav alanı) — ikisi de aynı gün düzeltildi (bkz. Kararlar → Haftalık Özet ekranı altındaki "Düzeltme" maddeleri). Ardından kullanıcı masaüstü özetini "kare" (2×2 ızgara, aynı gün-tipi ailesini yan yana) istedi — `groupByDayTypeFamily()` eklendi, hiçbir gün-tipi adı hardcode edilmeden sondaki `-N` deseninden aile çıkarımı yapılıyor. Sonra kullanıcı gün girişi ekranına (asıl set kaydetme ekranı) geçip 5 iyileştirme istedi: tekrar/rir alan genişlikleri, tekrar/rir'in sayısal seçiciye (native `<select>`) dönüşmesi, "nasıl gitti" sonrası akordiyonun otomatik ilerlemesi, planlanan bloğun görsel olarak ayrılması (henüz kilitlenmeden), rir varsayılanının 0 olması. Uygulamaya geçmeden önce gerçek bir çelişki ("tekrar" sayısal olunca "75sn"/"tükenene kadar" gibi gerçek kullanılan ifadeler actual tarafında girilemez hale gelirdi) AskUserQuestion ile kullanıcıya soruldu — kullanıcı "sadece sayı gir" dedi, buna göre uygulandı. Tüm değişiklikler yerel bir test harness'ıyla (gerçek bir gün kaydı oluşturup ekranı doğrudan render ederek) tek tek doğrulandı: select'lerin doğru aralıkta olduğu (tekrar 0-30, rir 0-9), carry-forward'ın select'lerle de çalıştığı, "nasıl gitti" sonrası doğru şekilde bir sonraki egzersize geçip kaydırdığı, planlanan bloğun amber çerçeveli göründüğü, rir'in 0 ile başladığı — hepsi gözlemlendi, konsol boyunca sıfır hata.
- 2026-08-08 (devam, aynı gün): Kullanıcı haftalık özet ekranındaki kırmızı "yapılamadı" göstergesinin sade emoji olduğunu, ayrıca hiç işaretlenmemiş egzersizlerin görünmez kaldığını fark etti — kırmızıya çerçeve, ayrıca turuncu zemin+beyaz tire ile üçüncü bir "nötr" (henüz işaretlenmedi) rozeti istedi. `util.js`'e paylaşılan `statusBadge()` eklendi, üç görüntüleme yeri (`dayEntry.js` kart başlığı, `weekSummary.js`, `weekSummaryDesktop.js`) aynı fonksiyonu kullanacak şekilde güncellendi. Yerel test harness'ıyla üç durumun da (yeşil ✓ / kırmızı ▼ / turuncu −) hem gün girişi ekranında hem özet ekranında doğru göründüğü doğrulandı. Bu sırada PROJECT.md'de daha önceki bir düzenlemenin yanlışlıkla iki alakasız maddeyi birleştirdiği fark edilip düzeltildi.
- 2026-08-08 (devam, aynı gün): Kullanıcı önce PROJECT.md'ye sadece kayıt düşülmesini istediği bir gelecek vizyonu detayı verdi (admin ekranı: hocaları ve öğrencilerini üstten takip; hoca ekranı: öğrenci tanımlama/ödeme takip/program ekleme) — "Gelecek Vizyonu" bölümüne eklendi, hiç kod yazılmadı. Ardından asıl istek: antrenman sırasında setler arası dinlenmeyi telefonun saatine geçmeden takip edebileceği, sürüklenebilir, floating bir kronometre. Kullanıcı kendi tasarımını önerip "nasıl sence" diye sordu; iki noktada netleştirme yapıldı (ikinci dokunuşta duraklat/devam ettir mi yoksa sıfırlama mı — duraklat/devam ettir önerildi; ekran değişince kaybolsun mu — kaybolmasın önerildi) ve kullanıcı ikisini de onayladı, ayrıca "kaybolmaması da uygun" diyerek sadece gün girişi ekranıyla sınırlı kalmasın, TÜM uygulamada kalıcı olsun istediğini netleştirdi. `js/components/restTimer.js` yazıldı — `#view-root`'un dışında `document.body`'ye ekleniyor, bu yüzden `router.js`'in her navigasyonda yaptığı `#view-root` temizliğinden etkilenmiyor. Gerçek `PointerEvent`ler simüle edilerek (bu bileşen `click` değil salt `pointerdown`/`pointermove`/`pointerup` dinliyor) çok hassas doğrulama yapıldı: tam saniye başına bir tık artış, duraklatınca donma, devam edince kaldığı yerden sürme, 700ms basılı tutmada anında sıfırlanma, büyük bir sürüklemenin konumu değiştirip başlat/duraklatı TETİKLEMEMESİ, ve `#view-root` içeriği tamamen değiştirildiğinde butonun DOM'da kalmaya devam etmesi — hepsi doğrulandı, konsol boyunca sıfır hata.
- 2026-08-09: Kullanıcı "roadmap planlayalım" dedi. Mevcut Gelecek Vizyonu + Sonraki Adımlar maddelerinden 6 maddelik bir aday listesi çıkarılıp kullanıcıya numaralandırılmış olarak sunuldu; kullanıcı önceliklendirmeyi kendisi yapmak istedi ("bana listedekiler var, bi numaralandır, ben ona göre sıra yapayım"). Verilen sıralama "6-5-3": Admin ekranı → Hoca ekranı → Haftalık Özet/Masaüstü Özeti iyileştirmeleri. Diğer maddeler (özel domain, "Planlanan (Hoca)" bloğunu kilitleme, çoklu-kullanıcı mimari temeli) kullanıcının isteğiyle sırasız backlog'da kalıyor ("diğerleri listede kalmaya devam edebilir. yarın bakıcaz"). Yeni "## Yol Haritası" bölümü eklendi. Hiç kod yazılmadı, sadece planlama/dokümantasyon.
- 2026-08-11: Kullanıcı gerçek kullanımdan sonra 11 maddelik bir iyileştirme/hata listesi verdi ve değerlendirilip sıraya konmasını istedi. Her madde teker teker değerlendirildi (bug/özellik/araştırma), iki madde netleştirme gerektirdi: "hayalet hafta" bug'ı (kullanıcı repro detayı vermek yerine "en son çözelim" dedi, listenin sonuna alındı) ve dinlenme kronometresinin bildirim alanında canlı görünmesi isteği (statik/backend'siz PWA'da güvenilir şekilde mümkün olmadığı açıklandı, kullanıcı bunun yerine kronometrenin çalışırken nabız gibi parlamasını istedi — ayrıca kullanıcının iPhone kullanmadığı öğrenildi, bu varsayım düzeltildi). Sonuç 9 maddelik sıralı bir backlog (bkz. "## İyileştirme Listesi"), kullanıcının açık kuralı: **tek seferde hepsi yapılmayacak, her madde uygulanıp doğrulanacak, onay alınca sıradakine geçilecek**. İlk madde uygulandı: `.rest-timer-btn` 64px→96px (1.5x), font 15px→22px, `running` durumunda `rest-timer-pulse` adında sürekli tekrarlayan bir box-shadow glow animasyonu eklendi (`--primary`'nin rgb karşılığıyla, 1.8sn döngü). `service-worker.js` v21'e çıkarıldı. Yerel bir test harness'ıyla (auth gerektirmeden `initRestTimer()` doğrudan çağrılarak) doğrulandı: yeni boyut/font doğru, `running` sınıfı eklenince `animationName: rest-timer-pulse` olarak computed style'da göründü, büyümüş boyutla sürükleme hâlâ doğru çalışıyor (çalışmayı kesmiyor), konsol boyunca sıfır hata.
- 2026-08-11 (devam, aynı gün): Kullanıcı 1. maddeyi canlıda kontrol edip onayladı ("temiz devam edebilirsin"). 2. madde (sticky header scroll düzeltmesi) uygulandı: `.exercise-card`'a `scroll-margin-top: calc(var(--header-h) + var(--safe-top) + var(--space-3))` eklendi — modern tarayıcılarda `scrollIntoView()` bu özelliği otomatik hesaba katıyor, `dayEntry.js`'in `advanceTo()` fonksiyonunda hiçbir JS değişikliği gerekmedi. Kök neden doğrulandı: sayfa `window` üzerinden scroll oluyor, `.app-header` `position:sticky; top:0` ile viewport'un tepesinde sabit kalıyor, `scrollIntoView({block:'start'})` kartı viewport y=0'a hizalıyordu, bu da header'ın kartın üst kısmını kapatmasına neden oluyordu. `service-worker.js` v22'ye çıkarıldı. Yerel bir test harness'ıyla (index.html'in header/nav/view-root iskeletini birebir taklit ederek, mobil viewport 375×812'de) doğrulandı: "Nasıl gitti?" işaretlenince açılan bir sonraki kartın üst kenarı artık header'ın 12px altında (`secondCardTop: 64`, header yüksekliği 52), `coveredByHeader: false` — ekran görüntüsüyle de teyit edildi, kart başlığı tam görünür durumda.
- 2026-08-12: Kullanıcı bir ekran görüntüsüyle 9 maddelik listeye 1 yeni madde ekledi: "Nasıl gitti?" butonları (dayEntry.js) `statusBadge()`'in solid renk/beyaz ikon stiliyle tutarsızdı (hâlâ eski emoji + translucent-tint), ve nötr seçeneği hiç yoktu. Küçük ve bağımsız olduğu için hemen (sıraya beklemeden) uygulandı: `.status-btn` aktif stilleri `var(--success)`/`var(--danger)`/`var(--warning)` solid renklere + beyaz ikona çevrildi, ikonlar rozetle aynı karakterlere (✓/▼/−) geçti, üçüncü bir `.status-btn-neutral` butonu eklendi. `setStatus()` zaten jenerik olduğu için nötr, mevcut toggle/auto-advance mantığına ekstra kod gerekmeden dahil oldu. `service-worker.js` v23'e çıkarıldı. Yerel bir test harness'ıyla doğrulandı: her üç durumun aktifken tam olarak `statusBadge()` ile aynı `rgb` arka plan + beyaz metin verdiği (computed style ile ölçüldü), sadece birinin aktif olabildiği (good→bad→neutral geçişlerinde öncekinin doğru şekilde pasifleştiği), ve kart başlığındaki küçük rozetin anlık senkron kaldığı — ekran görüntüsüyle de teyit edildi. **Not:** kullanıcı canlıda kontrol ettikten sonra "oldu galiba emin değilim, bu maddeye geri dönmek isteyebilirim" dedi — kapatılmadı, gerekirse revize edilecek şekilde açık bırakıldı, şimdilik 3. maddeye geçildi.
- 2026-08-12 (devam, aynı gün): 3. madde (tekrar/rir için özel seçici) uygulandı. Native `<select>`'in kökten çözülemeyen iki UX sınırı (aynı değeri tekrar seçince `change` olayının ateşlenmemesi + dropdown'un erişilemez konumda açılması) `setRows.js`'teki alanların `picker.js`'in bottom-sheet desenini kullanan özel bir sayı ızgarasına (`openNumberPicker()`) çevrilmesiyle çözüldü — ayrıntılar için bkz. "## İyileştirme Listesi" altındaki 3. madde notu. `service-worker.js` v24'e çıkarıldı, yerel test harness'ıyla hem re-select bug'ının hem carry-forward'ın hem de kapatma/vazgeçme akışlarının doğru çalıştığı hassas şekilde doğrulandı. Kullanıcı PC'de gördü ama telefonda henüz yansımadı, bilinen SW gecikmesi olarak not edilip devam edildi.
- 2026-08-12 (devam, aynı gün): 4. madde (antrenman başlat/durdur + süre takibi) uygulandı — ayrıntılar için bkz. "## İyileştirme Listesi" altındaki 4. madde notu. Özetle: `dayEntry.js`'e yeni bir "▶ Antrenmana Başla" → canlı süre → "Günü Tamamla" ile kilitlenen süre akışı eklendi, süre zaman damgasından hesaplanıyor (arka plana almaya/kapatmaya dayanıklı), `setInterval` navigasyonda kendi kendini `isConnected` kontrolüyle temizliyor. `service-worker.js` v25'e çıkarıldı, yerel test harness'ıyla (zaman damgası geriye alınarak "yeniden açma" simüle edilerek dahil) hassas şekilde doğrulandı.
- 2026-08-12 (devam, aynı gün): Kullanıcı 4. maddeyi yerel/canlı testin ötesinde salonda gerçek bir antrenmanda deneyeceğini söyledi ("bu maddeyi yarın sahada test edicem, burada park edelim, yarın devam edeceğiz kaldığımız yerden") — oturum burada duraklatıldı. **Kalan sıra değişmedi:** 5. Hoca notlarını ayrı alana taşı, 6. Not ikonu, 7. Egzersiz tipi (ağırlık/süre), 8. Geri sayım sayacı, 9. Hayalet hafta bug'ı (bkz. "## İyileştirme Listesi"). 1-4. maddeler push edildi ve (3. madde'nin telefon-cache gecikmesi dışında) canlıda doğrulandı.

**3. madde — Tekrar/Rir için özel seçici (2026-08-12):** Native `<select>`'in iki sorunu (aynı değeri tekrar seçememe — tarayıcı sadece değer GERÇEKTEN değişince `change` ateşliyor; dropdown'un erişilemez/üstte açılması — native select'in açılma konumu JS/CSS ile kontrol edilemiyor) kökten çözüldü: `setRows.js`'teki `<select>` bir `<button>` tetikleyiciye çevrildi, tıklanınca `picker.js`'deki `.sheet-backdrop`/`.sheet` desenini kullanan yeni bir `openNumberPicker()` açılıyor — ekranın altından açılan, 5 sütunlu bir sayı ızgarası (tekrar için 0-30, rir için 0-9), mevcut değer mavi vurgulu ve otomatik görünüme kaydırılmış. Herhangi bir hücreye (mevcut değer dahil) tıklamak `onSelect`'i tetikliyor — native select'in "değer değişmezse olay ateşlenmez" kısıtı burada yok, bu yüzden aynı değeri yeniden seçmek artık ilk denemede çalışıyor. `syncLaterRows()` artık `<button>` (textContent) ile `<input>` (value) arasında ayrım yapıyor. CSS'te `.number-select` → `.number-picker-trigger` olarak yeniden adlandırıldı, yeni `.number-picker-grid`/`.number-picker-cell` stilleri eklendi. `service-worker.js` v24'e çıkarıldı. Yerel test harness'ıyla hassas şekilde doğrulandı: aynı değeri (0) yeniden seçmek artık `touched:true`'ya geçip "suggested" stilini doğru şekilde temizliyor (önceki bug tam olarak buydu), farklı bir değer (3) seçmek de doğru çalışıyor, carry-forward yeni butonlu satırlara da doğru yayılıyor, arka plana/Kapat'a tıklayınca değer değişmeden kapanıyor, mobil ekran görüntüsünde ızgara ekranın altında tam erişilebilir konumda — konsol boyunca (bilinen Firebase test-harness uyarısı dışında) sıfır hata.

**Not (aynı gün):** Kullanıcı PC'de yeni seçiciyi gördü ama telefonda henüz yansımadığını bildirdi ("cache heralde. devam") — bilinen service worker güncelleme gecikmesi (bkz. Bilinen Notlar), kod tarafında bir sorun değil, ayrıca araştırılmadı, kullanıcı "devam" dedi.

**4. madde — Antrenman başlat/durdur + süre takibi (2026-08-12):** Kullanıcının isteği: "antrenmana başla" gibi bir buton, "Günü Tamamla" dendiğinde o sayaç dursun, günün antrenmanının ne kadar sürdüğü tutulup gösterilsin (raporlama entegrasyonu — haftalık özete yansıtma — kapsam dışı bırakıldı, ileride ayrı bir istek olarak gelecek). Yeni iki alan (`entry.workoutStartedAt`, `entry.workoutDurationSec`) — `createDayEntry`'de hiç ilklendirilmedi, tıpkı mevcut `entry.completed` gibi sadece truthy-check ile okunuyor (eski kayıtlarda `undefined`, kod açısından `null` ile aynı davranıyor, ekstra normalizasyon gerekmedi). `dayEntry.js`'e tarih kartıyla egzersiz listesi arasına yeni bir kart eklendi, üç durumu var: başlamadı (▶ Antrenmana Başla butonu) → çalışıyor (canlı `MM:SS` sayaç, `formatDuration()` ile `util.js`'e eklendi) → tamamlandı (kalıcı süre gösterimi). **Kritik tasarım kararı:** geçen süre, bellekte sayaç tutmak yerine her tick'te `Date.now() - workoutStartedAt`'tan HESAPLANIYOR — bu yüzden uygulama arka plana alınsa, sekme kapatılıp yeniden açılsa bile doğru kalıyor (dinlenme kronometresinin aksine, ki o bilinçli olarak bellekte tutuluyordu). "Günü Tamamla"nın süreye etkisi: SADECE ilk kez açılırken (henüz süre kilitlenmemişse) süreyi hesaplayıp kilitliyor; sonradan kapatılıp tekrar açılsa bile süre bir daha değişmiyor — yanlışlıkla tekrar tamamlama/geri alma süreyi bozamıyor. `setInterval`'in navigasyonda sızmaması için (router.js'te hiçbir unmount/lifecycle mekanizması yok) her tick'te `elapsedEl.isConnected` kontrol ediliyor, view değişip eleman DOM'dan sökülünce bir sonraki tick'te kendi kendini `clearInterval` ile durduruyor — router.js'e hiç dokunulmadı. `service-worker.js` v25'e çıkarıldı. Yerel test harness'ıyla doğrulandı: başlatma → canlı tik-tak (gerçek zamanla doğru arttığı ölçüldü) → `workoutStartedAt`'ı geçmişe alıp "yeniden açma" simülasyonunda geçen sürenin sıfırdan değil doğru yerden başladığı (65sn geriye alındı, "1:05" gösterdi) → tamamlama ile süre kilitlenmesi (115sn → "1:55") → tekrar aç/kapa ile sürenin DEĞİŞMEDİĞİ → navigasyon simülasyonunda elemanın `isConnected:false` olduğu ve bir sonraki tick'te sessizce durduğu (hata yok) — hepsi doğrulandı, konsol boyunca (bilinen Firebase uyarısı dışında) sıfır hata.

- 2026-08-13: Oturum devam etti ("Selam, devam edebilirsin"). 4. maddenin sahada test edilmesi ertelendi ("yarın denicem onu"), bu diğer maddeleri bloklamadığı için 5. maddeye (hoca notlarını ayrı alana taşı) geçildi. Kök neden: `bulkParse.js`'in parantez-içi metni (ağırlık açıklaması hariç) hep `note` diye döndürüp `bulkAdd.js`'in `commitBlocks()`'unda doğrudan `inst.note`'a yazması — hocanın program metnindeki ifade ile sporcunun kendi yazdığı not (dayEntry.js'in not textarea'sı) AYNI alanı paylaşıyordu. Çözüm: `bulkParse.js`'te `note` → `coachNote` olarak yeniden adlandırıldı (mantık aynı), `bulkAdd.js`'in önizleme alanı da `ex.coachNote`'a bağlanıp placeholder'ı "Hoca notu (opsiyonel)" oldu, `commitBlocks()` artık `coachNote`'u üçüncü pozisyonel argüman yerine `prescribed` objesinin içine koyuyor (`addExerciseInstanceWithPrescribed`/`buildInstance` imzaları sadeleşti, athlete `note` artık her zaman boş başlıyor). `dayEntry.js`'in amber çerçeveli "Planlanan (Hoca)" bloğuna, 4'lü alan ızgarasının altına tam genişlik bir "Hoca Notu" satırı eklendi — mevcut jenerik `.presc-input` wiring'i (data-field okuyup `updateInstancePrescribed` çağıran) yeni kod gerekmeden bu alanı da kapsadı. Node ile `parseExerciseLine` doğrudan test edildi (gerçek bir hoca-ifadeli satır → doğru `coachNote`, ağırlık-açıklama parantezi → hâlâ `coachNote` boş kalıyor, regresyon yok), ardından yerel bir tarayıcı test harness'ıyla uçtan uca doğrulandı: ayrıştırılan `coachNote` → `prescribed.coachNote`'a doğru yazıldı, sporcunun ayrıca yazdığı bağımsız bir not `inst.note`'ta hiç karışmadan kaldı, "Hoca Notu" input'u doğru değeri gösterip düzenlenebildi, konsol boyunca sıfır hata. `service-worker.js` v26'ya çıkarıldı.
- 2026-08-13 (devam, aynı gün): Kullanıcı 5. maddenin gerçek doğrulamasını da öbür haftaya erteledi (gerçek hoca metni gerektiği için), ama bunun diğer maddeleri bloklamadığını belirtip 6. maddeye (not ikonu) geçilmesini istedi. `dayEntry.js`'in egzersiz kartı başlığına, isim ile durum rozeti arasına küçük bir 💬 göstergesi eklendi — `inst.note` (sporcu notu) VEYA `inst.prescribed.coachNote` (hoca notu) ikisinden biri doluysa görünüyor, ikisi de doluysa yine TEK ikon (madde 5'in iki-alanlı yapısıyla doğal olarak uyumlu, ekstra ayrım gerekmedi). Kart her zaman aynı header'ı render ettiği için (açık/kapalı fark etmeksizin) ek koşullu mantık gerekmedi. `service-worker.js` v27'ye çıkarıldı. Yerel test harness'ıyla 4 durumun hepsi (notsuz, sadece sporcu notu, sadece hoca notu, ikisi birden) doğrulandı — ikon tam beklenen egzersizlerde görünüp diğerlerinde hiç görünmedi, ekran görüntüsüyle de teyit edildi, konsol boyunca sıfır hata.
- 2026-08-13 (devam, aynı gün): 7. madde (egzersiz tipi: ağırlık/süre) uygulandı — listedeki en büyük madde, önce 3 soruluk bir netleştirme turu yapıldı (ayrıntılar için bkz. "## İyileştirme Listesi" altındaki 7. madde notu). Özetle: `exercises.isDuration` kütüphane-seviyesinde yeni bir alan, `rir` süre-tipinde "rezerv saniye" anlamına gelip artık 0-9'a kırpılmıyor, tekrar/rir alanları süre-tipinde mevcut ağırlık-stepper deseniyle (genelleştirilip 3 alan arasında paylaşılan) 5sn adımlı steppers'a dönüşüyor, bulk-parse "Nsn" deseninden YENİ egzersizlerin tipini otomatik ayarlıyor (var olanları asla değiştirmeden), kütüphane ekranına elle değiştirme toggle'ı ve haftalık özet ekranlarına doğru "sn" etiketleri eklendi. Kod incelemesi sırasında `updateInstancePrescribed()`'in yeni `isDuration` parametresini unuttuğu fark edilip test edilmeden önce düzeltildi. Node + tarayıcı test harness'ıyla uçtan uca (kırpma davranışı, stepper mekaniği, regresyon-yok kontrolü, kütüphane toggle'ı, bulk-parse otomasyonu, özet ekranı etiketleri) doğrulandı, konsol boyunca sıfır hata. `service-worker.js` v28'e çıkarıldı.
- 2026-08-13 (devam, aynı gün): 8. madde (geri sayım sayacı) uygulandı, yeni madde eklenmeden önceki backlog'un son çalışma maddesi (bkz. "## İyileştirme Listesi" altındaki 8. madde notu). Yeni `js/components/countdownTimer.js`, `openCountdown({targetSeconds,label})` — ortalanmış bir modal, hazırlık(3sn)→sayım→bitti fazları, son 10sn'de kırmızı+ölçeklenen "intense" nabız animasyonu. Süre-tipi (madde 7) set satırlarına bir ▶ tetikleyici eklendi, o satırın GÜNCEL süre değeriyle açılıyor. Aktifken arka plana dokunma iptal etmiyor (sadece bittiğinde), aynı anda tek sayaç açık kalabiliyor. Ses/titreşim bilinçli olarak bu turda yok. Tarayıcıda uçtan uca gerçek zamanlı faz geçişleri izlendi, intense/done CSS'i izole doğrulandı, tek-aktif-sayaç ve backdrop-güvenlik davranışları test edildi, konsol boyunca sıfır hata. `service-worker.js` v29'a çıkarıldı.

**7. madde — Egzersiz tipi: ağırlık/süre (2026-08-13):** Kullanıcının isteği: bazı hareketler (örn. Hyperextension) ağırlıksız, süre tutarak yapılıyor; "tekrar" alanı artık sayısal seçici olduğu için (bkz. 2026-08-08 kararı) süre değerleri (75sn gibi) hiç girilemiyordu. Büyük bir değişiklik olduğu için önce 3 soruluk bir AskUserQuestion turu yapıldı, kullanıcının cevapları basit ikili seçeneklerden daha zengin çıktı, sentezlenip özet olarak geri sunuldu ve onaylandı ("Tamam öyle olsun, deneyelim, deneyime göre bakarız"). Nihai tasarım:
  - **Tip, egzersiz KÜTÜPHANESİNDE yaşıyor** (`exercises.isDuration`, varsayılan `false`) — instance-bazlı değil. Kullanıcının kendi çözümü: aynı hareketin ağırlıklı/ağırlıksız versiyonları gerekirse "Hyperextension" / "Hyperextension with kg" gibi AYRI kütüphane kayıtları olarak yönetilecek — ağırlık alanı zaten her egzersizde var/opsiyonel olduğu için ek bir "ağırlık da var mı" bayrağı gerekmedi.
  - **`rir` alanı süre-tipinde "Rezerv (sn)" anlamına geliyor** — aynı veri alanı (`rir`), aynı depolama, sadece süre-tipinde 0-9'a SIKIŞTIRILMIYOR (kullanıcının kendi örneği: "hedef 50sn'ydi, 10sn daha yapardım" — 10, eski 0-9 sınırının dışında). `buildActualSetsFromPrescribed`/`addActualSet`/`updateInstancePrescribed` üçü de artık `isDuration`'a bakıp `clampRir()`'i (0-9) atlıyor, sadece negatifi engelliyor.
  - **Girdi şekli: stepper** (mevcut ağırlık alanındaki −/+ deseni), adım 5 saniye — kullanıcının "typing yorucu, ızgara hantal" tepkisine cevaben önerildi, kabul edildi. `setRows.js`'teki tek-alan `buildWeightStepper` genelleştirilip `buildStepper(field,value,{step,unit})` oldu, kablo mantığı da (`wireRow`) artık TEK bir jenerik `.stepper` döngüsüyle çalışıyor (`data-step` attribute'undan adımı okuyarak) — ağırlık, süre, rezerv üçü de aynı kodu paylaşıyor. Süre-tipi olmayan egzersizler madde 3'teki sayı-ızgarası seçiciyi (`.number-picker-trigger`) aynen kullanmaya devam ediyor, hiç dokunulmadı.
  - **Bulk-parse otomasyonu:** `bulkParse.js`'in zaten var olan `TIME_RE` ("Nsn") tespiti artık `detectedDuration` bayrağı olarak dışa açılıyor (`reps` de artık "75sn" değil çıplak "75" — birim etiket/stepper'dan geliyor, tıpkı ağırlığın "kg" birimi gibi). `bulkAdd.js`'in `commitBlocks()`'u SADECE yeni oluşturulan bir egzersiz için `exercises.add(name, detectedDuration)` ile tipi otomatik ayarlıyor; zaten eşleşen bir egzersizin tipini ASLA değiştirmiyor (sürpriz olmasın diye — kullanıcının "2 versiyon" çözümüyle tutarlı).
  - **Kütüphane ekranına** (`js/components/libraryList.js`, hem exercises hem dayTypes'ın paylaştığı bileşen) opsiyonel bir `showDurationToggle` parametresi eklendi, sadece `exerciseLibrary.js` bunu açıyor (dayTypes etkilenmedi) — her satırda bir ⏱ toggle butonu, `exercises.setDuration(id, bool)` ile elle değiştirilebiliyor.
  - **Rapor ekranları da güncellendi:** `weekSummary.js`/`weekSummaryDesktop.js`/`dayEntry.js`'in "son sefer" satırı artık süre-tipi egzersizlerde "X tekrar"/"rir X" yerine "Xsn"/"rezerv Xsn" gösteriyor — aksi halde hoca'ya gidecek rapor yanıltıcı olurdu.
  - **Bulunup düzeltilen bir bug:** `updateInstancePrescribed()` (planlanan alan düzenlenince, hiç dokunulmamış actual set'leri yeniden üreten fonksiyon) `buildActualSetsFromPrescribed()`'i yeni `isDuration` parametresi olmadan çağırıyordu — kod incelemesi sırasında fark edilip düzeltildi, test edilmeden önce.
  - **Kapsam dışı bırakılan:** bulk-add önizleme ekranındaki alan etiketleri ("Set"/"Tekrar"/"Rir") süre-tipi satırlar için hâlâ genel — önizleme kısa ömürlü/hızlı bir ekran olduğu için şimdilik dokunulmadı.
  `service-worker.js` v28'e çıkarıldı. Doğrulama: node ile `parseExerciseLine`'ın hem süre hem ağırlık desenlerini doğru ayırt ettiği teyit edildi; tarayıcıda uçtan uca — süre-tipi bir prescribed'ta `rir:"10"`'un KIRPILMADIĞI, süre-tipi olmayanın hâlâ `rir` 0-9'a kırpıldığı, prescribed-block etiketlerinin doğru değiştiği ("Süre (sn)"/"Rezerv (sn)"), süre-tipi set satırlarının 3 stepper (ağırlık/süre/rezerv) gösterdiği ve +/- butonlarının 5'er adımla doğru çalıştığı (75→80→70), süre-tipi OLMAYAN bir egzersizin hâlâ tam olarak eskisi gibi (sayı-ızgarası, "Tekrar"/"Rir" etiketleri) çalıştığı (regresyon yok), kütüphane toggle'ının iki yöne de doğru çalıştığı, gerçek bir toplu-yapıştırma metninden ("Plank tut 3 set 50sn") YENİ bir egzersizin otomatik `isDuration:true` ile oluştuğu ama VAR OLAN bir eşleşmenin tipinin değişmediği, ve haftalık özetin süre-tipi egzersizlerde doğru "sn" etiketleriyle göründüğü — hepsi doğrulandı, konsol boyunca sıfır hata.

**8. madde — Geri sayım sayacı (2026-08-13):** Kullanıcının orijinal isteği: plank gibi süreli hareketlerde 50sn seçip 3-5sn gecikmeli başlatan, bitince (ses/titreşim bilinçli olarak bu turda YOK — kullanıcı "sonraya kalabilir" dedi) bir geri sayım. Ayrıca daha önceki bir netleştirmede (dinlenme kronometresinin nabız animasyonu tartışılırken) "son 10sn'de yoğunlaşan nabız" fikri bilinçli olarak buraya ertelenmişti. Ek bir AskUserQuestion turu açılmadı — mekanikler zaten önceki konuşmalarda netleşmişti, tasarım kararları doğrudan uygulanıp raporlandı:
  - Yeni bağımsız bileşen `js/components/countdownTimer.js`, `openCountdown({targetSeconds, label})` — `picker.js`/`openNumberPicker` gibi `document.body`'ye eklenen bir `.sheet-backdrop` (ama `align-items:center` ile ORTALANMIŞ bir modal, alttan açılan sheet değil — plank pozisyonundan bakınca büyük/net okunması için).
  - Üç faz: **hazırlık** (sabit 3sn, "Hazırlan..." + geri sayım), **sayım** (hedef süreden 0'a), **bitti** (✓ + "Bitti!", yeşil). Son 10sn'de `.countdown-intense` sınıfı ekleniyor — büyük sayı kırmızıya dönüp saniyede bir ölçekleniyor (`countdown-pulse` animasyonu), madde 1'in dinlenme kronometresindeki sürekli/sakin nabızdan kasıtlı olarak farklı (o modda hedef yok; burada hedefe yaklaşma hissi isteniyor).
  - **Nereden tetikleniyor:** her süre-tipi (madde 7) set satırının "Süre" stepper'ının yanına küçük bir ▶ butonu eklendi (`setRows.js`), tıklanınca O SATIRIN O ANKİ (canlı, en son +/- ile değişmiş) süre değeriyle açılıyor — egzersiz adı da (dayEntry.js'ten `exerciseName` olarak thread edildi) modal başlığında gösteriliyor.
  - **Güvenlik/UX detayları:** aktif sayım sırasında arka plana (backdrop) yanlışlıkla dokunmak ASLA iptal etmiyor (sadece "bitti" fazındayken dokunma kapatıyor, ya da elle "İptal" butonu) — parmak/kol antrenman sırasında ekrana çarparsa sayaç sürpriz şekilde kaybolmasın diye. Aynı anda tek bir sayaç açık kalabiliyor (yeni biri açılırsa eskisi sessizce kapanıp yenisiyle değişiyor, üst üste yığılmıyor). Süre-tipi OLMAYAN egzersizlerde ▶ butonu hiç görünmüyor.
  - `service-worker.js` v29'a çıkarıldı, precache listesine `countdownTimer.js` eklendi.
  Doğrulama: gerçek zamanlı fazlar (hazırlık→sayım→bitti) tarayıcıda uçtan uca izlendi (kısa hedef sürelerle, ör. 3sn/15sn), geçiş noktaları (hazırlığın "Süre" etiketine ve doğru başlangıç değerine geçişi, son-10sn'de intense sınıfının eklenmesi, bitişte ✓/yeşile dönmesi) doğru sırayla gözlemlendi; intense/done state'lerin CSS'i (kırmızı+animasyon, yeşil) izole class-toggle testiyle kesin doğrulandı; backdrop'a dokunmanın aktifken kapatmayıp sadece bittiğinde kapattığı, "İptal"in her fazda çalıştığı, ikinci bir sayaç açmanın öncekini üst üste yığmadan değiştirdiği, ▶ butonunun stepper'daki GÜNCEL değeri (test: 50→55 yapılıp açıldı, 55'ten saymaya başladığı doğrulandı) kullandığı, ve süre-tipi olmayan egzersizde ▶ butonunun hiç görünmediği — hepsi doğrulandı, ekran görüntüsüyle de teyit edildi (ortalı, büyük, net modal), konsol boyunca sıfır hata.

**Ara not (2026-08-13):** Kullanıcı 7. maddenin gerçek kullanıma nasıl yansıyacağını sordu — "yeni hafta yapıştırınca mı aktif olur?" Netleştirildi: HAYIR, bulk-parse otomatik tipi SADECE hiç görülmemiş yeni bir egzersiz adı için ayarlıyor; zaten kütüphanede olan bir egzersizin (örn. mevcut "Hyperextension") tipini elle, Diğer > Egzersizler ekranındaki ⏱ toggle'ından değiştirmek gerekiyor — ve işaretlenince anında (bu haftanın kalan günleri dahil, henüz o egzersiz işlenmemişse) etkili oluyor, çünkü tip instance'a değil kütüphane kaydına bağlı.

**Oturum duraklatıldı (2026-08-13):** Kullanıcı "temiz test yaparız şimdi karışmasın" diyerek 9. maddeyi (hayalet hafta bug'ı) de hafta sonuna erteledi. **Backlog'un 8/9 maddesi uygulanıp push edildi**, kalan tek madde (9) repro detayı bekliyor. 4-8 arası maddelerin gerçek sahada/kullanımda doğrulanması da kullanıcı tarafından zamana yayılmış durumda (bkz. ilgili madde notları) — hiçbiri "sorunlu" değil, sadece henüz gerçek antrenmanda denenmedi.

## Görsel Yön Değişikliği (2026-08-14, deneme aşamasında — henüz push edilmedi)

Kullanıcı "genel olarak UI iyileştirmesi nasıl yaparız, daha premium ama minimalist bir hale getirsek, şu an daha çok prototip gibiyiz" dedi. Süreç: önce hiçbir gerçek kodu değiştirmeden bir Artifact (öncesi/sonrası mockup, haftalık program + gün girişi ekranları, gerçek CSS değerleriyle "önce" birebir kopyalanıp yanına yeni yön kondu) hazırlanıp kullanıcıya gösterildi, onay alındı ("renk seçimleri fena değil... hareket adının öne çıkarılması... menü ve logo da iyi"). Kullanıcı bir kullanılabilirlik sorunu yakaladı — mockup'ta tekrar/rir alanları düz metin gibi duruyordu, tıklanabilir olduğu belli değildi — chip+ok(⌄) ikonuyla düzeltilip mockup güncellendi. Sonra kullanıcı "bunu deneyelim, beğenmezsek geri alabilir miyiz, local'de deneyeyim" dedi.

**Kapsam kararı:** Renk paletini ve nav ikonlarını (emoji → çizgi SVG) **globalde** değiştirdik (CSS değişkenleri paylaşıldığı için otomatik her ekrana yayılıyor). Yapısal değişiklikleri (kutu yerine çizgili liste, tıklanabilir chip, "Hoca" paneli, segmented "Nasıl gitti" kontrolü) SADECE mockup'ta gösterilen iki ekrana (haftalık program, gün girişi) uyguladık — diğer ekranlar (panel, geçmiş, ödemeler, kütüphaneler, özet ekranları, toplu ekleme) yeni renklerle ama eski kutu/kart yapısıyla kaldı, onaylanırsa sırayla geçilecek.

**Palet (`:root`, style.css):** Grafit zemin (#121316), sıcak kemik-beyazı metin (#EDEEF0), tek vurgu — eski pirinç/bakır (#C9A15A, üzerinde koyu metin #17130B — beyaz yerine, kontrast için). Durum renkleri kaldı ama daha az doygun: success #6FA287 (soluk adaçayı), danger #C1665A (soluk tuğla), warning #C0863C (soluk kehribar — hem "nötr" rozeti hem dashboard'un "dikkat" rozeti için, hem de artık vurgu rengiyle (bakır) karışmasın diye bilerek biraz farklı bir amber tonu seçildi). Yeni: `--border-strong` (Hoca panelinin üst çizgisi için) ve `--font-mono` (ui-monospace ailesi — TÜM ölçülen sayılar: kg, tekrar, rir, tarih, kronometre/geri sayım ekranları artık bu fontu kullanıyor, "ölçüm cihazı" hissi için).

**Yapısal değişiklikler:**
- `.exercise-card`/`.week-slot`: kutu (arka plan+border+radius) yerine ince üst/alt çizgili liste öğesi. "Bugün" vurgusu artık tam çerçeve değil, `::before` ile 3px'lik sol bir "ray" (rail).
- `.prescribed-block`: amber çerçeveli kutu kayboldu — artık sade bir üst çizgi + küçük bakır renkli "HOCA" etiketi ("Planlanan (Hoca)" metni kısaltıldı). 4 alan (ağırlık/set/tekrar/rir) hâlâ GERÇEK, DÜZENLENEBİLİR `<input>` — ama görsel olarak büyük tabular-mono sayı + altında küçük birim etiketi (label/input DOM sırası `flex-direction:column-reverse` ile tersine çevrildi, HTML'e dokunulmadı), arka plan/çerçeve şeffaf, sadece odaklanınca (`:focus`) bakır bir alt çizgi beliriyor. **Bilinçli olarak salt-okunur yapılmadı** — bu hâlâ ayrı, önceden not edilmiş bir karar (bkz. Kararlar → 5 iyileştirme maddesi #6).
- Tekrar/Rir seçici tetikleyicisi (madde 3'ün `.number-picker-trigger`'ı): artık tabular-mono + sağında küçük bir ok (⌄) — CSS `background-image` data-URI ile, JS'e hiç dokunulmadan. Kullanıcının "tıklanabilir gibi durmuyor" geri bildirimi üzerine mockup'ta eklenen chip+ok deseni buraya birebir taşındı.
- "Nasıl gitti?" 3 butonu artık ayrık değil, negatif margin ile üst üste bindirilmiş TEK bir segmented control gibi görünüyor (`:first-of-type`/`:last-of-type` ile sadece uç köşeler yuvarlak) — yine JS/HTML değişmeden, saf CSS.
- 🗑/💬 emoji'leri `util.js`'e eklenen paylaşılan `ICON_TRASH`/`ICON_NOTE` (currentColor kullanan çizgi SVG) ile değiştirildi — libraryList.js, dayEntry.js (×2), payments.js.
- index.html: alt nav emoji'leri (🏠💪📅☰) çizgi SVG oldu, header "Gym Takip" yerine "GYM" + bakır renkli "BN" wordmark'ı.

**Önemli bir kod incelemesi bulgusu, düzeltildi:** `.badge-warning`/`.badge-danger`/`.day-complete-btn.active` ve dinlenme kronometresinin nabız animasyonu, ESKİ renklerin sabit `rgba(...)` değerlerini kullanıyordu (CSS `var()`'dan değil) — yeni palet ile bu sabit değerler otomatik güncellenmiyordu. Hepsi yeni renklerin rgb karşılıklarıyla elle güncellendi (özellikle dinlenme kronometresinin nabız halkası artık eski maviyi değil yeni bakırı gösteriyor).

**Doğrulama:** Yerel bir test harness'ıyla (gerçekçi veriyle haftalık program + gün girişi + dashboard render edilerek) computed style üzerinden hassas şekilde doğrulandı: tüm renk token'ları doğru güncellendi, haftalık slot'ların arka planı şeffaf/çizgili hale geldi, "bugün" ray'i doğru renkte, Hoca panelinin input'ları hâlâ düzenlenebilir (değer korunuyor) ve tabular-mono gösteriyor, nötr durumun koyu metin/amber zemin kontrastı doğru, segmented "nasıl gitti" kontrolünün buton aralıkları birleşti, ikon SVG'leri doğru yerleşti, dashboard gibi DOKUNULMAYAN ekranlar da yeni renkleri sorunsuz aldı — konsol boyunca sıfır hata. (Not: bu oturumda tarayıcı ekran görüntüsü alma aracı kararsız davrandı, bu yüzden doğrulama tamamen computed-style/DOM sorgularıyla yapıldı — CSS token değişiklikleri için görsel karşılaştırmadan daha kesin bir yöntem.)

**Durum: Kullanıcı yerelde denedi, onayladı ("gayet güzel bence bunu yansıtabiliriz") ve push edildi.** Test sırasında bir "Run in Terminal" yanlış tıklaması `git revert e6a262b`'yi yarım bıraktı (HEAD e6a262b'de kaldı ama index/çalışma dizini eski içeriğe döndü) — `git status`/`git diff --cached` ile teşhis edilip `git reset --hard HEAD` ile e6a262b'ye net şekilde geri dönüldü, hiçbir kayıp olmadı. **Sıradaki adım:** diğer ekranlara (panel, geçmiş, ödemeler, kütüphaneler, özet ekranları) yapısal dilin sırayla yayılması — henüz başlanmadı, ayrı bir istek/oturumda ele alınacak.

- 2026-08-14: Kullanıcı "bu hali güzel, şimdi ne durumdayız genel olarak" diyerek genel durum özeti istedi, ardından Admin + Hoca ekranlarını planlamaya geçildi ("admin ve hoca ekranını planlayabiliriz... admin olarak hocaları tanımlıcam... hoca ekranında da hoca öğrenci tanımlıcak, ona program atayacak"). Plan Mode'da bir Plan-agent turu (roller/şema/güvenlik kuralları/başarısızlık senaryoları/onboarding akışı) + 3 netleştirme sorusu (gerçek hoca mı: evet; mevcut hesap etkilensin mi: HAYIR, istisna kalsın; admin ayrı hesap mı: evet) sonrası plan onaylandı, aynı gün **Faz A tamamen kodlandı ve yerelde doğrulandı** — ayrıntılar için bkz. "## Admin + Hoca Ekranları, Faz A" (bu bölümün hemen altında).
- 2026-08-15: Kullanıcı admin hesabını (`[REDACTED-UID]`) kendi oluşturdu, `admins/{uid}` dokümanını Console'dan elle ekledi. Agent, kullanıcının bağlı Chrome oturumu üzerinden Console'daki gerçek `firestore.rules` metnini okudu, taslakla karşılaştırdı (mevcut `users/{userId}` davranışı birebir korunuyor), publish etti; ardından kodu commit `354fab0` ile push etti. `admin.html` canlıda doğrulandı. Kalan tek adım kullanıcının kendi girişini yapması (agent'ın sınırı: gerçek şifre/hesap girişi hiç yapılmaz) — bkz. "## Admin + Hoca Ekranları, Faz A".

## Admin + Hoca Ekranları, Faz A (2026-08-14)

### Bağlam
Roadmap'in en üst iki maddesi (bkz. Yol Haritası). Kullanıcının netleştirdiği kapsam: Admin önce (hoca tanımlama + hoca listesi + öğrenci sayıları — bu kadarı yeterli), sonra Hoca Faz A (öğrenci tanımlama + program atama — **mevcut manuel `#/bulk-add` yapıştırması da AYNI ANDA çalışmaya devam etmeli**, öğrenciden bu özgürlüğü almak ayrı/sonraki bir karar). Hoca raporları (Faz B) ve ödeme takibi (Faz C) bilerek bu fazın dışında bırakıldı.

**En sert kısıt, kullanıcının kendi sözleriyle:** *"yeni sistem deyince korkuyorum, benim hesabım bir istisna olarak kalsın."* Kullanıcının mevcut gerçek hesabı/verisi bu işten hiç etkilenmiyor — bu yeni sistemde bir hocanın öğrencisi DEĞİL. Admin rolü için de ayrı, yeni bir hesap istendi (mevcut hesabın uid'i yeni koleksiyonlarda hiç görünmesin diye).

### Mimari
**Üç yeni statik giriş noktası** (`admin.html`, `coach.html`, `join.html`) — mevcut `index.html`/`js/app.js`/`js/auth.js`/`js/cloudSync.js`/`js/router.js`/`js/views/*`/`js/storage.js` altındaki HİÇBİR dosyaya (davranış olarak) dokunulmadı; tek değişen mevcut dosya `service-worker.js` (precache listesi + `CACHE_NAME` v31). Yeni öğrenciler `users/{uid}/data/main` şemasını AYNEN kullanıyor — bu sayede mevcut TÜM ekranlar yeni bir öğrenci için hiçbir değişiklik gerekmeden çalışıyor. Roller Firestore'da koleksiyon-varlığıyla ifade ediliyor (custom claim değil — bu proje hiç Cloud Function kullanmadı, kırılmasın diye): `admins/{uid}`, `coaches/{uid}` (doc id = gerçek uid), `students/{uid}` (doc id = gerçek uid, `coachId` alanlı), `coachInvites/{token}`, `studentInvites/{token}` (`crypto.randomUUID()` token, `pending`→`claimed`).

**Katılım akışı:** roster ekranında isim girilip "Davet Et" denince client bir `*Invites/{token}` dokümanı oluşturuyor, `join.html#/coach|student/{token}` linkini gösterip kopyalanabiliyor (WhatsApp'tan elle gönderiliyor — bugünkü program-paylaşma alışkanlığıyla tutarlı). Davet edilen kişi linki açıp kendi e-posta+şifresini giriyor (`createUserWithEmailAndPassword` — şifreyi asistan hiç görmüyor/yazmıyor, mevcut kalıcı sınırla tutarlı), tek bir `runTransaction()` içinde rol dokümanı oluşturulup davet `claimed` işaretleniyor (ikisi birlikte, yarım kalmasın diye), sonra `./index.html`'e yönlendiriliyor — buradan itibaren TAMAMEN sıradan bir ilk-giriş (mevcut `pullRemoteIfNewer`/`saveState` akışı hiç özel kod gerektirmeden çalışıyor).

**İzolasyon (uygularken bulunup planın ötesine geçilen bir karar):** `admin.html`/`coach.html`/`join.html` mevcut `js/cloudSync.js`/`js/auth.js`/`js/storage.js`'i HİÇ import etmiyor — kendi `js/shared/firebaseClient.js`'leri var, **isimlendirilmiş bir secondary Firebase app + bellek-içi (`inMemoryPersistence`) auth** kullanıyor. Sebep: `storage.js` (planın "reuse edilebilir" dediği `uid()` fonksiyonu için düşünülmüştü) kendi içinde `cloudSync.js`'i import ediyor, o da kendi paylaşılan/`browserLocalPersistence` (sekmeler arası ortak) Firebase auth oturumunu başlatıyor — bu yeni sayfalardan biri import etseydi, aynı tarayıcıda index.html'in oturumuyla ayrışmama riski doğardı (kullanıcının "istisna kalsın" kısıtına doğrudan aykırı). Bulundu, `uid()` yerine 4 satırlık saf kopyası `assignProgram.js` içine kondu, storage.js'e hiç dokunulmadı.

### Yeni dosyalar
- `js/shared/firebaseClient.js`, `js/shared/loginForm.js` — (plan listesinde yoktu, izole/DRY bir eklenti) izole Firebase bağlantısı + admin.html/coach.html'in paylaştığı giriş formu.
- `js/roster/rosterUi.js` — asenkron roster listesi + "davet oluştur" akışı (link paneli, kopyala, bekleyen davetler, iptal) — `libraryList.js`'in `.list`/`.list-item` görsel dilini paylaşıyor.
- `admin.html` + `js/adminApp.js` + `js/admin/adminCloud.js` + `js/admin/coachRoster.js` — hoca listesi (canlı öğrenci sayısı `getCountFromServer` ile) + davet formu. Tek ekran, kapsam bilerek bu kadar.
- `coach.html` + `js/coachApp.js` + `js/coach/coachCloud.js` + `js/coach/studentRoster.js` — öğrenci listesi + davet formu, `js/router.js` reuse edilerek 2. bir rotaya (`#/assign/:uid`) yönlendiriyor.
- `js/coach/assignProgram.js` — `js/views/bulkAdd.js`'in AYNI yapıştır→ayrıştır→önizleme→onayla desenini, BAŞKA bir kullanıcının (`coachCloud.js` ile doğrudan okunan/yazılan) uzak state objesi üzerinde çalışacak şekilde uyguluyor; `bulkParse.js`/`util.js` değiştirilmeden reuse edildi, `storage.js`'in CRUD yardımcıları (yukarıdaki izolasyon nedeniyle) küçük, saf kopyalar olarak yeniden yazıldı.
- `join.html` + `js/joinApp.js` — ortak davet-kabul ekranı, hesap oluşturma transaction'ı yarıda kalırsa (`catch` bloğu) yarım kalan Auth hesabını `user.delete()` ile geri alıyor.

### Güvenlik kuralları (`firestore.rules`, yeni — bu repoda ilk kez version-control'e giriyor)
Mevcut kural (`users/{userId}/{document=**}`: `request.auth.uid==userId`) **hiç değişmedi**, sadece bir `||` ile genişledi: `isCoachOfStudent(userId)` (hoca + `students/{userId}.coachId==kendi uid'i`). Yeni koleksiyonların her biri kendi kapalı `create` kapısına sahip (kendi kendine rol yazma imkânsız): `coaches`/`students` create'i SADECE ilgili `*Invites/{token}`'ın `status=='pending'` olduğunu `get()` ile doğrulayarak izin veriyor; `students` create'i AYRICA davetteki `coachId`'nin yazılan `coachId` ile eşleştiğini kontrol ediyor (yoksa geçerli-ama-başka-hocanın davetiyle "yanlış" hocaya bağlanma açığı olurdu — tasarım sırasında bulunup kapatıldı). `students` `list` kuralı `resource.data.coachId==request.auth.uid` (Firestore bunu sorgunun GERÇEKTEN bu filtreyle geldiğini zorunlu kılıyor, filtresiz/başkasının id'siyle sorgu tamamen reddediliyor) — admin için ayrıca `isAdmin()` OR'u var (öğrenci sayılarını görebilmesi için, ilk taslakta unutulmuştu, test sırasında bulunup eklendi).

`firebase.json`/`firestore.indexes.json` de eklendi — sadece **yerel emulator testi** için, gerçek Firebase projesine bağlı/deploy edilmiş değil.

### Doğrulama
- **Firestore Emulator Suite ile kural testi (`@firebase/rules-unit-testing`, `demo-gymbn` sahte proje):** Bu makinede Java çok eskiydi (1.8, gereken 21+), taşınabilir bir JDK 21 zip'i SADECE oturum scratchpad'ine indirilip açıldı (sisteme kalıcı kurulmadı, PATH'e kalıcı eklenmedi). 28 senaryo TEK TEK test edildi, **28/28 PASS**: herkese açık davet okuma, mutlu yol (admin→hoca daveti→claim→hoca→öğrenci daveti→claim→program yazma→öğrenci kendi verisini okur/yazar), mevcut hesabın (`realUser`) hiç etkilenmediği, ve 4 başarısızlık senaryosunun HEPSİ (başka hocanın öğrencisine erişim, roster sızıntısı — filtresiz VE yanlış-filtre sorgusu, kendi kendine rol yazma — davetsiz VE sahte-token VE coachId-sahteciliği, davet tekrar kullanma — hem coach hem student tarafı) doğru reddedildi.
- **`assignProgram.js`'in commit mantığı (Node, izole):** aynı fonksiyonlar (parse→enrich→assign→commit) gerçek kullanıcının önceden doğrulanmış 8-egzersizlik/2-günlük program metniyle (PROJECT.md'deki bulk-add örneği) test edildi — **15/15 PASS**, "(15x15kg)" ağırlığa gitmesi, "Hyper extansion"ın set sayısız çalışması, süre-tipi `reps` çıkarımı gibi ince noktalar dahil; kritik olarak AYNI programın 2. kez "yanlışlıkla" onaylanmasında egzersiz/gün-tipinin KOPYALANMADIĞI (normalize-edilmiş isimle eşleştiği) doğrulandı.
- **Canlı sayfa yükleme (Browser pane, yerel sunucu):** `admin.html`/`coach.html`/`join.html` açılıp konsolda sıfır hata doğrulandı (tüm yeni modüllerin import'ları network sekmesinden tek tek 200 OK olarak teyit edildi); `join.html`'in geçersiz-token hata yolu GERÇEK bir isteğe karşı da denendi (kurallar henüz Console'a yayınlanmadığı için `permission-denied` dönüyor, kod bunu doğru yakalayıp "Davet yüklenemedi" mesajı gösteriyor — beklenen/güvenli davranış).
- **Yapılamayan:** gerçek admin/coach hesaplarıyla canlı uçtan uca akış — bu, kuralların Console'a yayınlanmasından ve kullanıcının kendi admin hesabını oluşturmasından önce mümkün değil (bkz. altta).

### Kullanıcının şimdi yapması gerekenler
1. Ayrı, yeni bir Firebase Auth hesabı oluşturması (Admin rolü için — Console'dan, kendi belirlediği şifreyle; asistan bu adımı yapmaz, standart sınır).
2. Bu yeni hesabın uid'i için Firebase Console'dan elle bir `admins/{uid}` dokümanı oluşturması (asistan Console'da adım adım yönlendirebilir — Firestore'a doküman eklemek, hesap/şifre oluşturmaktan farklı, daha önce de bu şekilde yapılmıştı).
3. `firestore.rules`'ı Console'daki GÜNCEL/gerçek kural metniyle karşılaştırıp yayınlaması (asistan kör kör üzerine yazmıyor — mevcut kuralın `request.auth.uid==userId` olduğu varsayılıyor ama Console'dan teyit edilmeli).
4. Gerçek hocasının e-posta adresini bilmesi (davet linkini WhatsApp'tan kendisi gönderecek).

### Durum
**Kod tamamen yazıldı ve yerelde (emulator + Node testleriyle) doğrulandı. HENÜZ push edilmedi, Console'a hiçbir şey yayınlanmadı, gerçek bir admin/coach hesabıyla hiç denenmedi.** Sıradaki adım kullanıcının onayı + yukarıdaki 4 madde.

## İkinci Tur İyileştirmeler (2026-08-14)

Kullanıcı gerçek kullanımdan sonra 4 yeni UX maddesi verdi, aynı "tek seferde hepsi değil, biri yapılıp doğrulanıp onaylanınca diğerine geçilir" kuralıyla (bkz. kalıcı hafıza `feedback_iterative_batches`). Bu turda ayrıca yeni bir kalıcı kural netleşti: **tasarım/görsel etkisi olan her madde için önce canlı bir demo/örnek gösterilip onay alınıyor, gerçek koda ondan sonra dokunuluyor** (bkz. kalıcı hafıza `feedback_show_demo_before_design_changes` — bu ilkenin kendisi de kalıcı hafızaya `feedback_motion_feedback_principle` olarak kaydedildi: her etkileşimli öğe durum değişiminde görsel/hareketle tepki vermeli, statik/cansız kalmamalı).

3 maddenin (2, 3, 4) demoları tek bir Artifact'te (gerçek uygulamanın `:root` renk/font değişkenleriyle birebir) gösterildi, kullanıcı üçünü de inceleyip onayladı — madde 3'te iki seçenekten (A: sol ray, B: dolgu etiket+ikon) **B**'yi seçti ("B gibi, bi öyle deneyelim" — kesin değil, beğenmezse A'ya dönülebilir).

1. **Dinlenme kronometresi: 1dk pasif kalırsa otomatik sıfırlanma.** Kullanıcının sorunu: bir seti bitirip kronometreyi durdurunca, bir sonraki sete girerken hâlâ eski süreyi gösteriyordu, elle uzun-basıp sıfırlamak gerekiyordu. Mevcut uzun-basış sıfırlamasına DOKUNULMADI, ikincil bir mekanizma eklendi: `js/components/restTimer.js`'te kronometre DURAKLATILDIĞINDA (çalışırken değil) 60sn'lik bir "idle" `setTimeout` kuruluyor, süre dolana kadar tekrar başlatılmaz/sıfırlanmazsa kendini sıfırlıyor. `js/util.js`'e `isRestTimerAutoResetEnabled()`/`setRestTimerAutoResetEnabled()` (localStorage `gymbn_restTimerAutoReset`, cihaza özel — senkronlanan `gymbnData` blob'una bilerek dahil edilmedi, basit bir cihaz tercihi) eklendi. "Diğer" ekranına yeni bir "Ayarlar" bölümü + toggle switch eklendi (`js/views/more.js`, yeni `.setting-row`/`.settings-toggle` CSS'i — projede ilk gerçek toggle-switch bileşeni). **Varsayılan: açık** (kullanıcının kendi tercihi, AskUserQuestion ile soruldu — tarif ettiği sorunun doğrudan çözümü olduğu için). Görsel bir değişiklik olmadığı için demo gerekmedi. Yerel test harness'ıyla 5 senaryo doğrulandı: açıkken duraklatılıp bekleyince sıfırlanıyor, kapalıyken sıfırlanmıyor, ÇALIŞIRKEN (duraklatılmadan) asla sıfırlanmıyor, uzun-basış hâlâ bağımsız çalışıyor, toggle localStorage ile iki yönde de senkron — 5/5 PASS.
2. **İşaretlenen egzersiz ismi soluklaşsın.** Kullanıcının amacı: gözü henüz işaretlenmemiş egzersizlerde tutmak. Kapalı (`collapsed`) VE bir durumu (`inst.status`) olan kartlarda `.exercise-name` artık `var(--text-muted)`'a ve 600 ağırlığa geçiyor (`js/views/dayEntry.js`'de `buildExerciseCard`'ın class listesine `marked` eklendi, CSS'te `.exercise-card.collapsed.marked .exercise-name`). Durum rozetinin kendisi HİÇ soluklaşmıyor, sadece isim — sinyal (rozet) net kalırken, gözün gittiği yer değişiyor. Demo onaylandı ("tertemiz"), yerel test harness'ıyla renk değerleri computed-style ile doğrulandı (işaretsiz `--text`/rgb(237,238,240) → işaretli `--text-muted`/rgb(139,143,152), rozet rengi değişmedi) — 5/5 PASS.
3. **"Hoca" bloğu daha belirgin (Seçenek B).** Kullanıcının işaretlediği ekran görüntüsü üzerinden netleşti — hocanın verdiği hedefin göründüğü blok (`.prescribed-block`) çok sessiz kalıyordu. "HOCA" etiketi artık düz metin değil, dolgu bir rozet: `var(--primary)` (bakır) arka plan + `var(--primary-text)` (koyu) yazı + küçük bir "kişi" ikonu (`js/util.js`'e eklenen `ICON_COACH`, mevcut `ICON_TRASH`/`ICON_NOTE` deseniyle aynı stroke-tabanlı SVG dili). 4 alan (ağırlık/set/tekrar/rir) ve "Hoca Notu" hâlâ TAMAMEN düzenlenebilir — bu bilinçli bir değişiklik değildi (bkz. Kararlar → 5 iyileştirme maddesi #6, hâlâ ayrı, uygulanmamış bir karar). Reddedilen seçenek (A: sol ray) `.week-slot.today::before` ile aynı dili kullanıyordu ama kullanıcı B'yi tercih etti; kesin değil, A'ya dönüş kolay (tek CSS bloğu). Demo onaylandı, yerel test harness'ıyla renk/padding/border-radius/ikon boyutu computed-style ile birebir doğrulandı, ağırlık input'unun hâlâ düzenlenebilir olduğu (regresyon yok) teyit edildi.
4. **Onay animasyonu + haptik geri bildirim.** Kullanıcı bir GIF örneği attı (bu ortamın ekran görüntüsü aracı o an arızalıydı, dosya teknik olarak yüklendi ama görsel içeriği görülemedi) — bunun yerine 3 durağan kare ekran görüntüsü olarak paylaştı, bir onay rozetinin büyüyüp içindeki işaretin çizilerek belirdiği bir animasyon olduğu anlaşıldı. Uygulama:
   - `statusBadge(status, animate)` (`js/util.js`) artık düz metin karakterleri (✓/▼/−) yerine stroke tabanlı SVG path'ler kullanıyor (`stroke-dasharray`/`stroke-dashoffset` ile "çizilebilir" olması için) — `animate` parametresi (varsayılan `false`) verilmezse davranış TAMAMEN eskisiyle aynı (weekSummary.js/weekSummaryDesktop.js hiç değişmedi, hâlâ statik rozet gösteriyorlar). SADECE `js/views/dayEntry.js`'de kullanıcı bir "Nasıl gitti?" durumunu YENİ seçtiğinde (`justSetStatusInstId` — tek seferlik, bir sonraki `renderCards()`'ta tüketilip sıfırlanan bir işaretleyici) o kartın rozeti `animate:true` ile render ediliyor: rozet 0'dan büyüyüp hafif taşıyor (`status-badge-pop`), içindeki çizgi 140ms gecikmeyle "çiziliyor" (`status-badge-draw`), ve durumun rengiyle eşleşen bir halka dışa doğru genişleyip kayboluyor (`status-badge-ring`).
   - **"Günü Tamamla"** tıklanınca (sadece tamamlanma yönünde, geri alırken değil): buton hafifçe zıplıyor (`.just-completed`, `day-complete-bounce`) + merkezden 8 küçük renkli nokta (`--success`/`--primary`/`--warning` karışık) rastgele açılarda dışa doğru dağılıp soluyor (confetti — `js/views/dayEntry.js`'in yeni `playCompleteCelebration()`'ı, DOM'a her tıklamada yeniden oluşturuluyor ki art arda tamamla/geri al/tamamla'da animasyon her seferinde tazelensin).
   - **Haptik** (`vibrate()`, `js/util.js` — `navigator.vibrate` sarmalayıcısı, yoksa/hata verirse sessizce hiçbir şey yapmıyor, Android'de çalışır iOS Safari'de çalışmaz): kısa tek titreşim (15ms) durum seçilince, antrenman başlatılınca, kronometre uzun-basışla sıfırlanınca (pasif otomatik sıfırlamada TETİKLENMİYOR — kullanıcının elinde olmayan bir anda titreşmesin diye); "Günü Tamamla"da biraz daha belirgin çift titreşim (`[15,50,15]`).
   - `prefers-reduced-motion: reduce` için tüm yeni animasyonları (pop/ring/bounce/confetti) kapatan bir `@media` bloğu eklendi.
   - `.status-btn` (asıl "Nasıl gitti?" tıklama butonları) küçük bir `:active { transform: scale(0.92) }` bastırma tepkisi aldı — ayrı bir madde değildi ama aynı "her etkileşim tepki versin" ilkesinin doğal bir uzantısı.
   Demo onaylandı ("tertemiz"). Yerel test harness'ıyla 19 senaryo doğrulandı: `statusBadge()`'in animate/statik varyantları doğru HTML üretiyor, `vibrate()` hata fırlatmıyor, bir durum işaretlenince o kartın rozeti `status-badge-pop`+ring ile render ediliyor, advanceTo (bir sonraki egzersize otomatik geçiş) regresyonsuz çalışıyor, İKİNCİ bir işaretlemede ÖNCEKİ kartın animasyonu TEKRARLAMIYOR (tek seferlik doğru çalışıyor), "Günü Tamamla" `just-completed` class'ı ve 8 confetti noktasını doğru üretiyor, geri alma celebration'ı tekrar tetiklemiyor — 19/19 PASS.

`service-worker.js` bu 4 madde boyunca v31→v35'e çıktı (yeni dosya eklenmedi, sadece mevcutlar değişti). Konsol boyunca tüm testlerde sıfır hata.

**Canlı doğrulama turu (aynı gün, push sonrası):** Kullanıcı canlıda test ederken madde 3'te iki gerçek uygulama hatası yakaladı — demoda onaylanan tasarımla gerçek kod TAM eşleşmiyordu: (1) ikon yanlıştı (demoda ok/bayrak, kodda kişi silüeti — kodlarken kullanıcıya sormadan değiştirilmişti), (2) sayı değerlerinin kendisi demoda bakır renkteydi, kodda beyaz kalmıştı (sadece "HOCA" etiketi rozetleştirilmiş, değerlerin rengi unutulmuştu). İkisi de bulunup düzeltildi (`fdd8087`, `d5d3fb4`) — **ders:** onaylanan bir demo, o demonun TAM KENDİSİNİN onayı demek, "aynı fikirde" bir yeniden-yorum değil; gerçek koda geçerken demo HTML/CSS'i birebir referans alınmalı (bkz. kalıcı hafıza `feedback_show_demo_before_design_changes`'e eklenen not).

**"Yapılan (Actual)" set satırları — alan israfı düzeltmesi (aynı gün):** Kullanıcı gerçek bir egzersiz ekran görüntüsü üzerinden "kg ve tekrar alanları bu kadar yer kaplamalı mı" sorusunu sordu. Ölçüldü: `.stepper-input` (ağırlık) `width:100%` ile satırın tamamını dolduruyordu, `.number-field-reps`/`.number-field-rir` de `flex:3`/`flex:1` ile aynı şekilde geriyordu — kısa değerler (`64`, `10`, `0`) için çok fazla boş alan. Piyasadaki (Strong/Hevy/TrueCoach tarzı) set-log desenlerine bakılarak 3 alternatif hazırlanıp bir Artifact'te kullanıcının gerçek verisiyle (Chest supported vertical row, 64kg/2/10) kıyaslandı: (1) tek satır + ortak başlık [önerilen], (2) aynı 2 satır ama sıkılaştırılmış [en güvenli/küçük değişiklik], (3) piyasa usulü tam tablo (ağırlık da dokun-seç, stepper kaybediliyor). **Kullanıcı 2'yi seçti** ("en güvenli/küçük değişiklik"). Uygulama: `.stepper-input` `width:100%`→`84px`, `.number-field-reps`/`.number-field-rir`'in `flex:3`/`flex:1`'i kaldırılıp tetikleyici butonlara doğrudan `width:72px`/`56px` verildi, her iki satır da `justify-content:center` ile ortalandı — yapı/davranış (stepper, dokun-seç) hiç değişmedi, sadece boşluk düzeltildi. **Bulunan bir CSS özgüllük hatası:** `.stepper-input { width:84px }` ilk yazıldığında hiç etki etmedi — genel `input[type="text"] { width:100% }` kuralı (element+attribute, özgüllük 0,1,1) tek class seçiciden (0,1,0) daha güçlü çıktı; `.stepper .stepper-input` (0,2,0) olarak scope edilip düzeltildi, computed-style ile 84px olarak doğrulandı. Süre-tipi (isDuration) egzersizlerin steppers'ı (`.stepper` paylaşılan sınıf) da bu düzeltmeden otomatik faydalanıyor, ayrı kod gerekmedi.

**Sıradaki fikir (henüz sadece demo, uygulanmadı):** Kullanıcı "odağın yaptığım sette olması için diğer setleri soluk gösterme ya da akordiyon mantığıyla gizleme" fikrini attı, alternatifleriyle demo istendi — bkz. altta, henüz kod yazılmadı.

**Rapor ekranlarında hoca notu hiç görünmüyordu (aynı gün, ayrı bir düzeltme):** Kullanıcı masaüstü özeti ekran görüntüsünde "Not" sütununda hem kendi yazdığı hem hocanın notu gibi duran metni görüp "ayırmak lazım" dedi. Kod incelendiğinde asıl kök neden ortaya çıktı: `js/views/weekSummary.js` ve `js/views/weekSummaryDesktop.js` **`inst.prescribed.coachNote`'u (Hoca Notu) hiç göstermiyordu** — sadece `inst.note` (sporcunun kendi notu) render ediliyordu. (`coachNote`/`note` veri seviyesinde zaten 2026-08-13'te ayrılmıştı, bkz. Kararlar → Çoklu-cihaz senkron altındaki not — ama rapor ekranları o zaman güncellenmemişti.) Kullanıcının ekran görüntüsündeki "karışık" metin muhtemelen onun kendi `+ Not Ekle` alanına hem hocanın söylediğini hem kendi gözlemini elle birlikte yazmasından kaynaklanıyordu — çünkü Hoca Notu raporlarda hiç görünmüyordu, ayrı tutmanın bir faydası yoktu. **Düzeltme:** iki ekranda da, varsa `coachNote` bakır (`--primary`) renkte "Hoca: ..." (mobil) / "H: ..." (masaüstü, dar sütun) olarak, varsa `inst.note` mevcut soluk/italik stille "Sporcu: ..." / "S: ..." olarak AYRI satırlarda gösteriliyor; hiçbiri yoksa hiçbir şey basılmıyor (regresyon yok). Yerel test harness'ıyla 9 senaryo doğrulandı (ikisi de dolu, sadece hoca notu, hiçbiri yok — her iki ekranda da; hoca notu rengi computed-style ile bakır olarak teyit edildi) — 9/9 PASS, konsol sıfır hata. `service-worker.js` v41'e çıktı.

**Saha kullanımından 3 madde (aynı gün, tek seferde gönderildi):**
1. **Geri sayım modalı ekranın altında kalıyordu.** Kök neden bulundu: `.countdown-backdrop`'un ortalama kuralı (`align-items:center`) ile `.sheet-backdrop`'un (bottom-sheet'ler için) `align-items:flex-end` kuralı AYNI özgüllükteydi (ikisi de tek class seçici) — `.sheet-backdrop` dosyada DAHA SONRA tanımlandığı için kazanıyordu, modal görünmez şekilde alta itiliyordu. `.sheet-backdrop.countdown-backdrop` olarak birleşik seçiciye alınıp özgüllük kesin şekilde yükseltildi (0,2,0 > 0,1,1), kaynak sırasına bağımlılık ortadan kalktı. Test: modal merkezi Y = viewport yarısı (360=360), PASS.
2. **Hazırlık geri sayımı 3sn → 5sn.** `PRE_DELAY_SECONDS` sabiti değiştirildi, tek satır.
3. **Tekrar/Rir seçicisi yeniden düzenlendi.** İki ayrı istek birleşti: (a) hocanın hedefinin ızgarada görünmesi ("7-8" gibi aralıklarda alt sınır referans alınıyor — zaten `extractLeadingInt` bunu yapıyordu, artık `storage.js`'ten export edilip `setRows.js`'te de kullanılıyor), (b) 0'ın ızgaradan ayrılıp kendi geniş butonuna alınması (kullanıcı: "1'den başlarsa 1-5, 6-10 diye daha rahat takip edilir" — özellikle Rir'de 0 çok sık seçilen bir değer olduğu için görünür/erişilebilir kalması önemliydi). Sonuç: ızgara artık 1'den başlıyor (doğal 5'li satır grupları), 0 üstte ayrı bir buton + kesikli ayraç, hedef değer (seçili olandan FARKLIYSA) kesikli çerçeve + küçük nokta ile işaretleniyor — aynı anda hem seçili hem hedef olan bir hücrede çakışma olmasın diye `target` sadece `current`'tan farklıysa uygulanıyor. İki Artifact turundan geçti (önce sadece hedef işareti, sonra 0-ayırma+doğal gruplama eklendi), ikisi de onaylandı.
`js/storage.js`'ten `extractLeadingInt` export edildi (önceden module-private). Yerel test harness'ıyla 10 senaryo doğrulandı (0 ayrı/ızgara 1'den başlıyor, hedef+seçili farklı hücrelerde doğru işaretleniyor, hedef=seçili çakışmasında sadece "selected" görünüyor, 0 butonuna tıklama fonksiyonel, normal hücreye tıklama regresyonsuz) — 10/10 PASS, konsol sıfır hata. `service-worker.js` v42'ye çıktı.

## Canlı Kullanımın İlk Günü (2026-08-15)

Admin+Hoca Faz A canlıya alındıktan (bkz. yukarıda "Durum") hemen sonra, kullanıcı gerçek admin hesabıyla ilk uçtan uca denemeyi yaptı — bu sırada bir gerçek bug ve bir gerçek eksik-özellik-hissi bulundu, ikisi de aynı gün çözüldü.

**Bug: hoca daveti kabul edilince yanlış sayfaya düşüyordu.** Kullanıcı hoca daveti oluşturup kendi test hesabıyla kabul etti, ama `coach.html` yerine sporcu uygulamasını (`index.html`) gördü — "hocanın diğer menüleri nerede" diye sordu. Kök neden: `js/joinApp.js`'in `trySubmit()`'i, hesap oluşturma+claim başarılı olduktan sonra rolden BAĞIMSIZ olarak `location.href = './index.html'` yapıyordu — hoca da öğrenci de aynı yere yönlendiriliyordu. Firestore tarafı doğruydu (`coaches/{uid}` dokümanı transaction içinde doğru yazılmıştı), sadece son adımdaki yönlendirme yanlıştı. **Düzeltme:** `location.href = role === 'coach' ? './coach.html' : './index.html';` — tek satır. Kullanıcının zaten oluşmuş hoca hesabı yeniden davet gerektirmeden çalıştı, sadece `coach.html`'e gidip aynı e-posta/şifreyle giriş yapması yeterli oldu. `service-worker.js` v43'e çıktı.

**İyileştirme: Program Ata → Önizleme ekranının alan etiketleri ve giriş yöntemi.** Hoca ilk kez `assignProgram.js`'in önizleme ekranını kullanınca iki şey fark etti: (1) Ağırlık/Set/Tekrar/Rir alanlarının placeholder'ı değer girilince kayboluyor, hangi kutunun ne olduğu sütun sırasından başka bir şeyle belli olmuyordu; (2) Set/Tekrar/Rir'i öğrenci tarafındaki sayı seçici (setRows.js) gibi dokunarak seçmek istedi. İki Artifact turundan geçti (önce etiket+seçici demoları, sonra kullanıcının "B seçeneğini istiyorum fakat tekli de seçebileceksem" + "tükenene kadar checkbox'ı" istekleriyle refine edildi), ikisi de onaylandıktan sonra kodlandı:
- **Kalıcı etiketler:** `js/coach/assignProgram.js`'in `buildExerciseRow`'u artık her alanı `<div class="field"><label>...</label>...</div>` ile sarıyor (style.css: `.bulk-exercise-row-fields .field` — column-reverse + `margin-bottom:0`, dayEntry.js'in "Hoca" bloğundaki AYNI fikir ama bu ekranın kendi kutulu input görünümü korunarak — `prescribed-fields`'ın altı-çizili minimal stili DEĞİL, onaylanan demo buydu). Ağırlık hâlâ sıradan bir `<input>`.
- **Set için tekil seçici:** `openSetPicker()` — setRows.js'in `openNumberPicker`'ıyla aynı iskelet (target kavramı hariç), 1-20 arası + ayrı 0 butonu, tıklayınca hemen seçip kapanıyor.
- **Tekrar/Rir için aralık-veya-tekil seçici:** `openRangePicker()` — bir sayıya dokun=tekil aday, farklı bir sayıya ikinci dokunuş=aralık ("8-10"), aynı sayıya tekrar dokunursan tekile döner (state machine: `pick(i)` içinde `start==null || end!=null` ise sıfırla-ve-başlat, değilse `end`'i doldur). Bu, `bulkParse.js`'in gerçekten desteklediği (`RIR_RE`/`REPS_RE` `\d+(?:-\d+)?` kalıbı) aralık girişini korumak için gerekliydi — öğrenci tarafındaki tekil-değer seçici doğrudan taşınsaydı "7-8" gibi aralıkları yazamaz hale gelirdik.
- **"Tükenene kadar" checkbox'ı (SADECE Tekrar'da):** `bulkParse.js` taranıp doğrulandı — `UNTIL_FAILURE_RE` ("tükenene kadar") reps alanında tanınan TEK serbest metin, Rir (`RIR_RE`) hiçbir zaman metin almıyor. Checkbox işaretlenince ızgara `.grid-disabled` (opacity+pointer-events:none) ile pasifleşiyor, kapatınca alan direkt `"tükenene kadar"` alıyor.
- **Bilinen, bilerek ertelenen sınır:** süre-tipi (`isDuration`) hareketlerde bu alan saniye tutuyor (60-90+ gibi), ızgara 1-30 aralığına sığmayabilir — kullanıcıya söylendi, ayrı bir karar/iş olarak bırakıldı, bu turda dokunulmadı.
- Aynı `.bulk-exercise-row-fields`/`.bulk-ex-field` yapısını paylaşan `js/views/bulkAdd.js` (öğrencinin kendi yapıştırma ekranı) aynı etiket sorununu taşıyordu — kullanıcı onayladı ("olur"), aynı gün `buildExerciseRow`/`openSetPicker`/`openRangePicker`/sabitler birebir `bulkAdd.js`'e taşındı (eski `buildExerciseRow`'u portlamadan önce assignProgram.js'in düzeltme-öncesi haliyle birebir aynı olduğu teyit edildi). `bulkAdd.js` (izole assignProgram.js'in aksine) `storage.js`'i import ettiği için aynı tarayıcı test harness'ı tekrarlanmadı — `node --check` + dikkatli bir yapısal tekrar-okuma ile doğrulandı, mantığın kendisi zaten kardeş dosyada 22/22 + 21/21 kanıtlanmıştı.
- Kullanıcı iki ilgili maddeyi bilerek sonraya bıraktı: süre-tipi (`isDuration`) hareketlerde Tekrar alanının saniye tutup 1-30 ızgarasına sığmaması ("tamam bakarız sonra"), ve seçici hücrelerine `vibrate()` ile dokunsal "tık" eklenmesi ("vibrate de bakarız" — bkz. yukarıdaki öneri).

**Hoca'nın diğer ekranları (aynı gün, çok parçalı bir istek):** Kullanıcı, öğrenci tanımladıktan sonra sadece program atamanın yeterli olmadığını belirtti: "1) roster'a anlık arama + istatistik şeridi, 2) öğrenci yönetimi (beden ölçümleri+grafik, bu haftaki program, hareket progression, rapor), 3) ödeme takibi fikri." Kapsam büyük olduğu için parçalara ayrıldı (bkz. hafıza `gymbn_project`), kullanıcı önceliği "Beden ölçümleri + grafik" olarak seçti, ödeme takibi için önce demo istedi.

**Madde 1 — Roster arama + istatistik şeridi:** `js/roster/rosterUi.js` (admin.html VE coach.html'in paylaştığı bileşen) — "Öğrenci/Hoca adı" kutusu artık ÇİFT görev görüyor: submit'te yeni davet oluşturuyor (değişmedi), `input` olayında da hem eklenmiş hem bekleyen listeyi AYNI ANDA filtreliyor (sadece `.list-item-title` metnine göre — ilk denemede `row.textContent` kullanılmıştı, bu TÜM satırların aynı alt metnini ("Program atamak için dokun") de eşleştiriyordu, test yazarken yakalanıp `.list-item-title`'a daraltıldı). Sıfır eşleşmede "davet et"e yönlendiren bir ipucu satırı (`--warning` renkli) çıkıyor ki arama mı yeni ekleme mi karışmasın. Yeni, extensible bir `.stat-strip`/`.stat-tile` bileşeni (tek taş bugün — sayım — ileride yanına eklenebilir), `config.statLabel` ile parametrize (`coach.html`→"Öğrenci", `admin.html`→"Hoca"), her iki ekranda da bedava çalışıyor (paylaşılan bileşen). **Bulunan bir isim çakışması:** yeni `.stat-tile-value`/`.stat-tile-label` sınıfları, `dashboard.js`/`payments.js`'in zaten kullandığı `.stat-card .stat-value`/`.stat-card .stat-label` ile İSİM ÇAKIŞMASI yaratacaktı (bare `.stat-value` seçici tüm `.stat-card` içeriğine de sızardı) — kodlamadan ÖNCE fark edilip farklı isimlendirildi, `.stepper-input`/`.countdown-backdrop` özgüllük hatalarıyla aynı ailede bir tuzak. Gerçek `renderRosterScreen()`'i (sahte `loadItems`/`loadPendingInvites` ile, Firestore'suz) yükleyen bir tarayıcı testiyle doğrulandı (11/11 PASS — alt-metin çakışması dahil, büyük/küçük harf Türkçe duyarlılığı dahil), konsol sıfır hata. `service-worker.js` v47.

**Madde 2 — Öğrenci yönetim hub'ı + beden ölçümleri:** Roster satırına dokunmak artık doğrudan program atamaya gitmiyor — birden fazla yönetim ekranı (program/ölçüm/ödeme, ödeme henüz yazılmadı) eklendiği için yeni bir `js/coach/studentDetail.js` (`#/student/:uid`) hub ekranı araya girdi, üç ekrana da link veriyor. `assignProgram.js`'in back-link'i (`#/`den `#/student/:uid`'e) buna göre güncellendi.

`js/coach/studentMeasurements.js` (`#/measurements/:uid`) — daha önce (bu oturumun başında, brainstorm turunda) onaylanan "yansıyan diyagram" fikrinin birebir gerçek koda geçmiş hali: kadın/erkek toggle, altı ölçüm satırı (boy/kilo/göğüs/bel/kol/bacak — her biri ikon+son değer+son tarih), satıra dokununca hem diyagramda ilgili bölge ışıldıyor HEM tarih+değer giriş sheet'i açılıyor. Yeni veri alanı: öğrenci state'ine `measurements: []` (kayıt: `{id, type, date, value}` — `payments[]` ile aynı "düz, tarihli kayıt listesi" deseni). `storage.js`'in `defaultState()`'ine ve `assignProgram.js`'in izole `emptyState()`'ine eklendi; VAR OLAN öğrenci state'lerinde bu alan hiç olmayabileceği için okurken `state.measurements || []` ile savunmacı davranılıyor (migration yapılmadı, gerek yok). Cinsiyet seçimi kalıcı bir öğrenci alanı değil — sadece hangi diyagramın gösterileceğine dair oturum-içi bir görünüm tercihi, kullanıcı bunu istemedi.

Doğrulama: saf yardımcı fonksiyonlar (`recordsFor`/`latestFor`/`earliestDate`/`latestDate`) + gerçek `renderScreen()`'i (geçici `__test__` export'uyla, sahte öğrenci/state ile, Firestore'suz) yükleyen bir tarayıcı testiyle 24/24 PASS — boş/dolu durumlar, cinsiyet geçişi, satıra dokununca doğru bölgenin ışıması, geçersiz girişte kaydın ilerlememesi, kayıttan sonra doğru değerin görünmesi dahil. Geçici export ve test dosyaları push öncesi kaldırıldı, konsol sıfır hata. `service-worker.js` v48 (2 yeni dosya precache listesine eklendi).

**Ölçümlerin kendi ilerleme grafiği (brainstorm'daki "Fikir 3", aynı gün eklendi):** `studentMeasurements.js` ekranına, diyagramın altına bir "İlerleme" bölümü eklendi — sadece 2+ kaydı olan ölçüm tipleri için (tek kayıtla eğilim gösterilemez, o tip hiç kart almıyor), her kart: son değer + fark (ilk↔son kayıt, ok+miktar) + tarih aralığı + küçük bir çizgi grafik (`buildLineChart`'ın brainstorm'daki tek-seri kopyası, dolgu alanı + son noktada vurgu halkası). **Bilinçli bir sadeleştirme:** orijinal brainstorm'daki `goodDir` (artış/azalış "iyi" mi) kavramı EKLENMEDİ — kilo için "azalış iyi" varsaymak kesme hedefli bir öğrenci için doğru, kütle hedefli biri için yanlış olurdu; uygulamada öğrencinin hedefini tutan bir alan yok, o yüzden ok+fark nötr renkte (`--text-muted`) gösteriliyor, yorum yapılmıyor. Yerel test harness'ıyla 14/14 PASS (tek kayıtta bölüm hiç render edilmiyor, 2+ kayıtlı tipler doğru kart alıyor, artış/azalış oku ve miktarı doğru, nötr renk teyit edildi, sparkline path'i doğru nokta sayısıyla çiziliyor) — konsol sıfır hata. `service-worker.js` v49.

**Madde 3 — Ödeme takibi:** `js/coach/studentPayments.js` (`#/payments/:uid`) — yeni bir tasarım/şema değil, öğrencinin kendi ekranlarındaki (dashboard.js'in döngü kartı + payments.js'in ekleme formu/geçmiş listesi) BİREBİR AYNI görsel dili, `.stat-card`/`.cycle-dots`/`.badge-danger` gibi hepsi zaten var olan CSS'i hiç değiştirmeden reuse ediyor. Döngü hesabı (`getPaymentCycleStatus`) storage.js'ten izole bir kopya (`cycleStatus()`) olarak tekrarlandı — aynı 4 haftalık/28 gün gecikme mantığı. Ekleme ve silme, `assignProgram.js`/`studentMeasurements.js` ile aynı "önce yerelde uygula, uzaktan yaz, başarısız olursa geri al" desenini kullanıyor — **silme zaten id'ye göre geri alıyordu, ekleme ilk yazımda `pop()` kullanıyordu (eş zamanlı bir silme araya girerse yanlış kaydı geri getirebilirdi), test yazarken fark edilip id'ye göre geri almaya çevrildi.** Yerel test harness'ıyla 20/20 PASS (döngü haftası matematiği — 1/2/3/4. hafta + 28 gün gecikme sınırı, birden fazla kayıtta en son tarihin baz alınması, ödemesiz/güncel/gecikmiş üç durumun doğru render'ı, tutarsız ödemede alt metnin hiç basılmaması, boş tarihle submit'in sessizce reddedilmesi) — konsol sıfır hata. `service-worker.js` v50.

**Madde 5 — Roster'a ödeme rozeti + istatistik şeridinin ikinci taşı (son parça):** `getPaymentCycleStatus()` mantığı (artık `studentPayments.js` ile 2. kez gerektiği için) `js/coach/paymentCycle.js`'e çıkarıldı — `extractLeadingInt` gibi minik yardımcıların aksine bu döngü matematiği tekrar edilmeye değecek kadar kendine has, o yüzden gerçek bir paylaşımlı modül oldu (ikisi de zaten aynı izole `js/coach/` ağacında, storage.js sınırını hiç aşmıyor). `js/roster/rosterUi.js` (paylaşılan bileşen) iki genel özellik kazandı: `item.badge` (satırın sağında `.badge`/`.badge-danger`/`.badge-warning`) ve `config.extraStats(items)` (istatistik şeridine ekran-özel ek taşlar) — ikisi de opsiyonel, admin.html'in hoca listesi hiçbirini kullanmıyor, davranışı birebir eskisiyle aynı kaldı (ayrıca test edildi). `studentRoster.js`'in `loadItems()`'ı artık her öğrencinin `payments[]`'ini (roster dokümanında değil `users/{uid}/data/main`'de olduğu için AYRI bir okuma, `Promise.all` ile paralel) çekip rozete çeviriyor: 1-3. hafta düz rozet, 4. hafta `badge-warning`, gecikince `badge-danger`+"Gecikti"; hiç ödeme kaydı yoksa rozet YOK (yanlış aciliyet izlenimi vermesin diye). İkinci stat taşı ("N Gecikmiş", `warn` sınıflı) SADECE en az 1 gecikmiş öğrenci varsa görünüyor.

Doğrulama: `badgeForCycle()` saf mantığı + gerçek `renderRosterScreen()`'i sahte rozetli öğrencilerle yükleyen bir tarayıcı testiyle 13/13 PASS (rozet metni/rengi doğru, ödemesiz öğrencide rozet yok, ikinci taş doğru koşulda görünüyor, rozet metni aramaya sızmıyor), AYRICA admin.html'in rozetsiz/extraStats'sız gerçek kullanımını simüle eden ayrı bir testle 5/5 PASS (geriye dönük uyumluluk — admin ekranı hiç değişmedi) — konsol sıfır hata. `service-worker.js` v51.

**Canlı testten iki gerçek hata daha, aynı gün:**
1. **Silme ikonları görünmüyordu — proje genelinde, sadece bugünkü ekranlarda değil.** Kullanıcı ödeme geçmişinde silme tuşu olmadığını fark etti; incelenince `ICON_TRASH` (util.js) SVG'sinin hiç `width`/`height` almadığı, `.btn-icon svg` için de projede HİÇ bir boyut kuralı olmadığı ortaya çıktı — `getBoundingClientRect()` ile ölçülünce ikon 0×0 render oluyordu (buton yine de vardı ve tıklanabiliyordu, sadece görünmezdi). `.btn-icon` içindeki DİĞER her şey düz karakter (×, ⧉, ✎, ‹, ›) olduğu için font-size'dan boyutlanıyordu, ICON_TRASH tek SVG içerikti — bu yüzden bug fark edilmeden 6 farklı yerde (roster davet iptali, gün/egzersiz silme, İKİ ödeme ekranı, kütüphane silme) aynı anda duruyordu. Tek satırlık paylaşılan bir kural (`.btn-icon svg { width:18px; height:18px; }`) hepsini aynı anda düzeltti — kullanıcının tam o sırada istediği "ortak yerden değiştirme" ilkesinin kendiliğinden bir örneği.
2. **İleri tarihli bir ödeme "−15 gün önce" gibi anlamsız bir değer gösteriyordu.** Kullanıcı test sırasında ileri tarihli (bugünden 15 gün sonrası) bir ödeme girmiş, döngü kartı negatif gün sayısı göstermişti. `daysSince` artık kaynağında `Math.max(0, ...)` ile sabitleniyor (hem `storage.js`'in orijinalinde hem `paymentCycle.js`'in kopyasında — ikisi de güncellendi, tek yerde bırakılmadı) VE tarih input'larına (hem hoca hem öğrenci ekranı) `max="bugün"` eklenerek ileri tarih seçimi kaynağında engellendi.

Doğrulama: ikon boyutu için gerçek DOM'da `getBoundingClientRect()` ölçümüyle önce/sonra karşılaştırması (0×0 → 18×18), döngü mantığı için sahte `Date.now()` ile 4/4 test (ileri tarihte artık 0 gün/1. hafta/gecikmemiş, geçmiş tarihte davranış aynı kaldı) — konsol sıfır hata. `service-worker.js` v52.

**Ödeme modeli baştan tasarlandı (aynı gün, kullanıcının canlı testinden çıkan bir gözlem üzerine):** Kullanıcı kendi Ödemeler ekranını denerken "4 haftalık periyodun ucu açık gibi geliyor" dedi ve net bir alternatif tarif etti: kayan 4 haftalık döngü yerine SABİT bir ayın günü — "hangi gün ödemeye başladıysa o artık milad olsun", geç ödense bile takvim günü kaymasın, son 10 günde geri sayım çıksın. Demo onaylandı ("güzel, uygulayalım böyle"), aynı gün uygulandı:
- **Yeni mantık** (`js/coach/paymentCycle.js` + `storage.js`'in `getPaymentCycleStatus()`'ı, ikisi de güncellendi): milad = EN ESKİ ödemenin günü (ayrı bir ayar yok). Sıradaki ödeme = EN SON ödemeden sonraki ilk milad günü — geç ödense bile bir sonraki miladı ERTELEMİYOR (kullanıcının "hep aynı gün" isteği), sadece o anki gecikmeyi kapatıyor. Ay sonu günleri (31 gibi) o ay yoksa ayın son gününe clamp ediliyor, yıl geçişi doğru işliyor. `weekInCycle`/4-nokta bar tamamen kalktı.
- **3 durum, 3 ekranda aynı şablon** (`dashboard.js`, `payments.js`, `studentPayments.js`): rahat (sadece tarih), geri sayım (son 10 gün, "N gün kaldı"/"Bugün"), gecikmiş ("Ödeme zamanı geldi" + Gecikti rozeti). Türkçe sayı-sonek uyumu sorunu (19'u/19'unda gibi her sayı için farklı ek) bilerek "Sabit ödeme günü: {gün}" gibi sonek gerektirmeyen cümle kalıplarıyla aşıldı.
- **Roster rozeti aynı 3 duruma güncellendi**: rahat = kısa tarih (`formatDateShortTr`, sonek sorunu yok), geri sayım = "N gün" + `badge-warning`, gecikmiş = "Gecikti" + `badge-danger` (değişmedi).
- `.cycle-dots`/`.dot`/`.dot.filled`/`.dot.overdue` CSS'i tamamen kaldırıldı (JS'te hiç referans kalmadığı grep ile doğrulandı).

Doğrulama: `paymentCycle.js`'in yeni mantığı için 14 senaryolu izole bir tarayıcı testi (14/14 PASS — tam ödeme gününde henüz gecikmemiş olması, erken ödemenin sıradaki tarihi KAYDIRMAMASI, geç ödemenin takvim gününü DEĞİŞTİRMEMESİ, 31→28 Şubat clamp'i, yıl geçişi, milad'ın en eski kayıttan gelmesi dahil), 3 kartın hepsi için sahte `cycle` nesneleriyle 13/13 PASS, roster rozeti için 5/5 PASS — hepsi geçici `__test__` export'larıyla, push öncesi temizlendi, konsol sıfır hata. `service-worker.js` v53.

**Native `confirm()` → uygulamanın kendi onay sheet'i, uygulama genelinde (aynı gün, "brand guideline" ilkesinin doğrudan uygulanışı):** Kullanıcı hoca ödeme ekranında silme işleminde "emin misin" modalı istedi — meğer ZATEN vardı, ama tarayıcının native `confirm()` popup'ıydı (uygulamanın kendi koyu temalı sheet diliyle tutarsız). Sadece o ekranı yamamak yerine, `confirm(` kullanılan TÜM 10 yeri (`rosterUi.js`, `bulkAdd.js`, `assignProgram.js`, `studentPayments.js`, `dayEntry.js`×2, `libraryList.js`, `more.js` — yedek geri yükleme, `payments.js`, `week.js`) taradım ve yeni `js/components/confirmSheet.js`'e taşıdım — tek tanım, `Promise<boolean>` dönen `confirmSheet(message, {confirmLabel, cancelLabel, danger})`, mevcut `.sheet-backdrop`/`.sheet` diline oturuyor. Silme gibi geri alınamaz aksiyonlar `danger:true` (kırmızı `.btn-danger`) + "Sil", "devam edilsin mi" gibi bilgilendirici onaylar `danger:false` (`.btn-primary`) + "Devam Et" gibi bağlama uygun etiketler aldı. 7 çağrı yeri senkron event handler içindeydi, `async` yapılması gerekti (3 tanesi zaten assignProgram.js/studentPayments.js/rosterUi.js'te async'ti).

**Bu tarama sırasında bulunan gerçek bir regresyon:** `week.js`'in hafta üstü küçük "Ödeme döngüsü: X. hafta" notu, bugün değiştirilen `getPaymentCycleStatus()`'ın ESKİ `weekInCycle` alanını okuyordu — yeni modelde bu alan hiç yok, sessizce "undefined. hafta" basardı. Ödeme modeli değişikliğinde bu üçüncü tüketiciyi (dashboard/payments/studentPayments'in yanında) gözden kaçırmıştım; `confirm()` taraması sırasında fark edilip aynı 3-durumlu modele (`Ödeme güncel`/`Ödemeye N gün`/`Ödeme gecikti`) güncellendi — henüz push edilmeden yakalandı.

Doğrulama: `confirmSheet()`'in kendisi için 12 senaryo (render, onay/vazgeç/backdrop-tıklama üç farklı sonuç, özel etiketler, danger:false→btn-primary) + gerçek `libraryList.js` silme akışının uçtan uca testi (sahte store ile: buton tıkla → sheet aç → mesaj doğru → onayla → `store.archive` gerçekten çağrıldı) — 15/15 PASS, konsol sıfır hata. Ayrıca `.weekInCycle`/`.lastDate`/`confirm(` için tüm `js/` ağacında son bir grep taraması yapıldı, başka kaçan yer olmadığı doğrulandı. `service-worker.js` v54.

**Madde: Hoca atadığı programı GÖREMİYORDU (aynı gün, kullanıcının canlı testinden):** Hub'daki "Program" linki sadece YENİ program atama (yapıştırma) akışına gidiyordu — ne o haftaki mevcut programı ne geçmiş haftaları görüntüleyen bir yol yoktu. Kullanıcı net bir mental model verdi: "öğrencinin takvimini görüyor gibi, tarihte geri/ileri gidebilir, tek fark sadece görüntüleyebilir." `js/views/weekSummary.js` buna neredeyse birebir uyuyordu — zaten TAMAMEN görüntüleme-amaçlı (öğrencinin kendi haftalık özeti, hiç düzenleme yok, soyulacak bir interaktiflik bulunmuyordu) — sadece veri kaynağını yerel singleton'dan uzak `state`'e çevirip `week.js`'in ‹/› hafta gezinmesini eklemek yeterli oldu. Yeni `js/coach/studentSchedule.js` (`#/schedule/:uid/:mondayIso`, ay opsiyonel = bu hafta), hub'a "Takvim" olarak eklendi; eski "Program" linki artık "Program Ata" (netlik için, artık ikisi ayrı kavram). **Sıfır yeni CSS gerekti** — `.summary-*` (weekSummary.js'ten) ve `.week-nav`/`.btn-icon` (week.js'ten) birebir reuse edildi.

Doğrulama: gerçek `renderScreen()`'i (geçici `__test__` export'uyla, sahte öğrenci/state, Firestore'suz) yükleyen bir tarayıcı testiyle 16/16 PASS — boş hafta, dolu hafta (prescribed/actual formatlaması, süre-tipi birim farkı, hoca+sporcu notlarının ayrı satırda kalması, tamamlanan gün ✅ işareti), hafta gezinme linklerinin doğru Monday'lere gitmesi, geri linkinin hub'a dönmesi, eksik alanlarla çökmemesi dahil — konsol sıfır hata. `service-worker.js` v55.

**Kalıcı ilke, kullanıcı tarafından net söylendi:** UI bileşenlerini bir "marka rehberi" gibi düşün — bir buton/alan bir yerde onaylandıysa aynısı HER yerde kullanılsın, değişiklik gerekirse ortak bir yerden tüm kullanımlara yayılsın. Bkz. kalıcı hafıza `feedback_shared_ui_components`.

**Hoca'nın diğer ekranları isteği tamamlandı** — roster arama+istatistik, öğrenci yönetim hub'ı, beden ölçümleri+ilerleme grafiği, ödeme takibi+roster rozeti, hepsi canlıda. Kullanıcı iki maddeyi bilerek sonraya bıraktı (bkz. yukarıda): süre-tipi hareketlerde ölçüm ızgarası ve seçici hücrelerine haptik dokunuş.

**Bug: hedef aralığın üst ucu sessizce kayboluyordu.** Kullanıcı ekran görüntüsüyle gösterdi: hocanın "3-4" gibi bir aralık prescribe ettiği bir Tekrar alanında, öğrenci zaten alt sınırı (3) seçmişse, sayı seçicisinde HİÇBİR kesikli hedef işareti görünmüyordu — "4"ün de hedef olduğu bilgisi tamamen kayboluyordu. Kök neden: `js/components/setRows.js`'in `.number-picker-trigger` handler'ı `extractLeadingInt(inst.prescribed[field], null)` kullanıyordu — bu SADECE aralığın İLK sayısını çıkarıyor (`"3-4"` → `3`), ikincisini hiç görmüyor. `target === current` (3 === 3) olduğunda kesikli işaretin `i !== current` koşulu gereği hiç basılmaması (aynı hücrenin hem dolu hem kesikli görünmesini önlemek için, bilerek öyle tasarlanmıştı) ile birleşince, aralığın üst ucu tamamen kayboluyordu. **Düzeltme:** yeni, yerel bir `extractIntRange(str)` (aralıksa İKİ sayıyı da `[low, high]` olarak döner, tekilse `[n]`, hiç sayı yoksa `[]`) — `openNumberPicker`'ın `target` parametresi `targets` (dizi) oldu, `targets.includes(i) && i !== current` ile HERHANGİ bir hedef sayı (current'a eşit olmayan) kesikli işaretleniyor. Tekil hedefler (aralıksız "7" gibi) davranış olarak birebir aynı kaldı — regresyon yok. `storage.js`'ten import edilen `extractLeadingInt` artık bu dosyada kullanılmıyor (storage.js'in kendi iç kullanımı için export'u kaldırılmadı). İzole Node testi (12/12 — kullanıcının bildirdiği tam senaryo dahil: prescribed "3-4"+current=3 → sadece 4 işaretli) ve gerçek `renderSetRows()`'u gerçek CSS ile yükleyen bir tarayıcı testi (7/7) ile doğrulandı, konsol sıfır hata. `service-worker.js` v46.

Doğrulama: `parseRangeValue`/`formatRangeValue`/`fieldDisplay` + pick() state machine için izole Node testi (22/22 PASS — aralık sırası ters girilirse düzeltiliyor, aynı sayı iki kez = tekile dönüyor, aralıktan sonra yeni dokunuş sıfırlayıp yeniden başlıyor gibi uç durumlar dahil), ayrıca gerçek `style.css` + gerçek `buildExerciseRow`'u (geçici `__test__` export'uyla) yükleyen bir tarayıcı test harness'ı (21/21 PASS — etiketlerin kalıcılığı, boş alanın "—"/`.empty` göstermesi, Set/Tekrar/Rir seçicilerinin açılıp doğru değerle kapanması, checkbox'ın ızgarayı pasifleştirip "tükenene kadar" taşıması, Rir sheet'inde checkbox'ın HİÇ olmaması, Ağırlık'ın hâlâ input olması) — geçici export ve test dosyası push öncesi kaldırıldı, konsol sıfır hata. `service-worker.js` v44'e çıktı.

**Hareket videosu + hedef bölge, ve egzersiz kartlarının kart-yığını görünümü (Fitonist ve Apple Wallet ekran görüntülerinden esinlenildi, üç Artifact demo turu sonrası onaylandı).** Dört parça tek akışta birleşti: (1) egzersiz kütüphanesine (`js/views/exerciseLibrary.js`) yeni bir simge — video linki + sabit 8'li hedef bölge listesinden (Göğüs/Sırt/Omuz/Kol/Bacak/Kalça/Karın/Tüm Vücut) seçim yapılan bir sheet açıyor (`js/components/libraryList.js`'e opt-in `showMediaEditor` parametresiyle eklendi, `js/storage.js`'e `exercises.setMedia()`, mevcut `openEntrySheet`/studentMeasurements.js kalıbı birebir kopyalandı: `document.createElement`+`document.body.appendChild`+backdrop-tıklamayla kapama); (2) `dayEntry.js`'in egzersiz kartları artık her biri kendi kabuğunda (yuvarlak köşe, gölge, `margin-top:-18px` ile hafif üst üste binme), sol kenarda hedef bölgeye göre renkli ince bir çizgi var (`util.js`'teki `EXERCISE_REGIONS` sabit renk haritası, CSS custom property `--region-color`, bölgesi olmayan egzersiz nötr `--border-strong`'a düşüyor); (3) genişletilmiş kartın içinde, "Son sefer" satırının üstünde hedef bölge etiketi + "▶ Hareketi Gör" düğmesi (not-ekle düğmesiyle aynı görsel dil — `.note-toggle`/`.video-toggle-btn` artık tek bir gruplu CSS seçicisi, eski kopya `.note-toggle` kuralı silindi) — tıklanınca linki yeni sekmede açan bir önizleme alanı beliriyor (gerçek video gömme yok, sadece dış linke yönlendirme — host'a özel oEmbed/parsing gerektirmiyor); (4) Diğer > Ayarlar'a üçüncü satır olarak eklenen bir anahtar bunun tamamını kapatabiliyor (`gymbn_showExerciseMedia`, `isRestTimerAutoResetEnabled` ile birebir aynı localStorage kalıbı). İkinci ayar satırı eklenince tekrar kullanılabilir bir ayraç gerekti — tek satıra özel inline style yerine genel `.setting-row + .setting-row` kuralı yazıldı, gelecekte üçüncü bir satır eklenirse hiç dokunmadan çalışır.

Üç demo turu gerekti: ilk turda kullanıcı yığın fikrinin Haftalık Program'a değil gün-içi egzersiz listesine ait olduğunu belirtti ("yanlış yeri yaptık"); ikinci turda (doğru yere taşınmış haliyle) renklerin gürültülü/tema-dışı olduğunu söyledi ("tasarım bence kötü oldu") — üçüncü turda gradyan kartlar (altın/yeşil/bordo dolgu) yerine tüm kartlar aynı `var(--surface)` yüzeyini kullanan, sadece kenar çizgisi renklenen sade hâle geçildi, onay bu sürüme geldi.

Doğrulama: gerçek `render()` fonksiyonlarını (exerciseLibrary/dayEntry/more, sahte state, Firestore'suz) yükleyen bir tarayıcı testiyle — kütüphane satırındaki simgenin aktif/pasif rengi ve 18×18 boyutu (ICON_TRASH'in eski 0×0 bug'ı tekrarlanmadı), sheet'in doğru ön-doldurulması/kaydetmesi/`exercises.byId()`'e kalıcı yazması, kartların gerçek `border-radius`/`box-shadow`/`--region-color` (ve bölgesiz egzersiz için doğru fallback), akordiyonun hâlâ tek-seferde-bir-kart kuralına uyması, üç veri kombinasyonunun (video+bölge / sadece bölge / hiçbiri) doğru satırı üretmesi, video düğmesinin href/target/rel/caption'ının doğru olması, ayarlar anahtarının kapatınca satırı gerçekten gizleyip `localStorage`'a yazması, silme düğmesinin (`stopPropagation` ile) akordiyonu yanlışlıkla açıp kapatmaması — hepsi doğrulandı, konsol sıfır hata; test harness'ı (`_test_exercisecard.html`) ve içindeki `window.__test__` push öncesi silindi. `service-worker.js` v56.

**Yanlış alarm: "kartlar telefonumda kupkuru görünüyor".** Kullanıcı canlıda yukarıdaki tasarımı göremediğini bildirdi — uzun bir önbellek/service-worker araştırması (cache temizleme, gizli pencere, DevTools Service Workers paneli, CDN header incelemesi, iki kez `service-worker.js` sürüm artırma) hepsi temiz çıktı, çünkü gerçekten bir önbellek sorunu yoktu. Kök neden, kullanıcının **gerçek Chrome'una Claude-in-Chrome uzantısıyla bağlanıp** kendi hesabını incelemekle bulundu: hiçbir gerçek egzersizi henüz hedef bölge etiketlenmemişti, o yüzden her kart aynı, neredeyse görünmez nötr `--border-strong` kenar çizgisini alıyordu — yeniden tasarım (yuvarlaklık, gölge, üst üste binme) doğru uygulanıyordu ama renksiz haliyle fark neredeyse hiç belli olmuyordu, özellikle küçük bir telefon ekranı görüntüsünde. Bir egzersizi canlı olarak etiketleyip sayfayı yenileyince net bir renkli çizgi belirdi, doğrulandı. **İki ders:** (1) bir görsel özellik "görünmüyor" dendiğinde ve sunucu tarafı doğrulama (curl, byte-diff) temiz çıkıyorsa, önbelleğe takılıp kalmadan özelliğin görsel sinyalinin kullanıcının henüz girmediği veriye bağlı olup olmadığını kontrol et. (2) Claude-in-Chrome uzantısı (sanal Browser pane'den ayrı, bu oturumda ekran görüntüsü alamayan) kullanıcının gerçek, oturum açmış tarayıcısını sürebiliyor — "bende çalışıyor, sende çalışmıyor" farkı çıktığında ilk başvurulacak araç bu olmalı.

**Aynı gün, kullanıcı onayladı ("evet güzel olmuş"):** egzersiz videoları artık dış sekmeye çıkmadan uygulama içinde gerçekten oynuyor. `util.js`'teki `youTubeEmbedId()` tek bir regex ile watch/shorts/youtu.be/embed URL biçimlerinden video ID'sini çıkarıyor; `dayEntry.js`, tanınan YouTube linkleri için statik önizlemeyi gerçek bir `youtube-nocookie.com/embed/{id}` iframe'iyle değiştiriyor (tembel yükleme — sadece oynat düğmesine dokununca, `{once:true}` dinleyiciyle oluşturuluyor), YouTube olmayan linkler eskisi gibi dış sekmede açılmaya devam ediyor. Kullanıcının gerçek etiketlenmiş egzersiziyle (`dumbell shoulder press`) Claude-in-Chrome üzerinden doğrulandı — gerçek YouTube oynatıcısı arayüzü kartın içinde belirdi, ekran görüntüsüyle teyit edildi. `service-worker.js` v58.

**Dinlenme kronometresi jestleri ve boyu yeniden tasarlandı.** Kullanıcı üç Artifact demo turuyla netleşti: (1) tek dokunuş yine anında başlat/durdur (gecikme yok); (2) çift dokunuş (300ms içinde) artık sıfırlıyor — eski uzun-basış sıfırlamasının yerini aldı; (3) uzun basış (600ms) artık büyük/küçük boy arasında geçiş yapıyor (`gymbn_restTimerBig`, `localStorage`'da kalıcı, `isRestTimerAutoResetEnabled` ile aynı kalıp); (4) sürükleyerek taşıma değişmedi. Boyutlar kullanıcının netleştirdiği kesin değerler: küçük = bugünkü 96px (değişmedi), büyük = tam 2 katı, 192px, `width`/`height`/`font-size` üzerinde 0.25s geçişle büyüyüp küçülüyor. Çalışırken gösterilen animasyon da yeniden tasarlandı — kullanıcıya dört alternatif (nabız/ripple/nefes/dönen halka) gösterildi, **ripple (iki halka art arda dışa yayılan)** seçildi ve kullanıcının isteğiyle küçük boyda da aynı efekt kullanılıyor (eskiden sadece box-shadow tabanlı tek bir nabız vardı). **Mühendislik detayı:** çift-dokunuş klasik "ikinci dokunuşu bekle sonra karar ver" yöntemiyle DEĞİL, "ilk dokunuşu anında uygula, 300ms içinde ikincisi gelirse geri alıp sıfırla" şeklinde kuruldu — en sık kullanılan tek-dokunuşa hiç gecikme eklenmedi, bedeli sadece çift dokunulduğunda kısa bir görsel titreşim. Buton artık `btn.textContent` yerine iç `.rest-timer-time` span'ine yazıyor ki her saniye render ripple halkalarını silmesin. Claude-in-Chrome ile gerçek `PointerEvent` dispatch'leriyle (gerçek zamanlamayla — sentetik `.click()` değil) uçtan uca doğrulandı: tek dokunuş başlat/durdur, 130ms arayla çift dokunuş sıfırlama, 750ms basılı tutma boy değiştirme + kalıcılık, hepsi canlı hesapta çalışır durumda bulundu ve test sonrası temizlendi. `service-worker.js` v59.
